# Feature Critique — ledger

One row per critique run. Read this before picking a target (skip anything listed
here unless the user points at it on purpose); append to it at the end of every
run, including rejected critiques.

| Date | View | Feature | Compared to | Verdict | Decision | Outcome |
|------|------|---------|-------------|---------|----------|---------|
| 2026-07-23 | Results / ResultView | Bookmark → save to collections | Spotify add-to-playlist sheet | B− flow wearing an A-tier feature set | Adjusted — "user must pick a destination every time" | Shipped — PR #68 |
| 2026-07-23 | Results / ResultView | Where To Watch availability table | JustWatch title-detail "Where to watch" | C+ presentation wrapped around an A− dataset | Adjusted — global atlas is the point (don't localise it away); no deep links (users just want to know which country has it); free-to-watch only is deliberate, rent/buy stays excluded | All 8 shipped — PR #70, verified on release APK on the A54 |
| 2026-07-23 | Results / ResultView | Cast & Crew rail (grouping, See All overflow, long-press peek sheet) | IMDb title page "Top cast" + "Cast & crew" full page | A-tier cards on a rail with no information architecture — B for any single card, D for everything past card ten | Approved | All 8 shipped — PR #71, verified on release APK on the A54; user chose full-cast screen for #2 and left #5's affordance to me (persistent corner badge) |
| 2026-07-23 | Results / ResultView | Seasons rail (TV titles) — inert cards, no header, no season-scoped availability | Apple TV app series page (season selector + per-season provider attribution) | A-tier data pipeline feeding a rail of coasters — B+ for the fetch, D for the section | Approved | All 7 shipped — verified on release APK on the A54 (Squid Game 3-season, Chernobyl 1-season, Parasite movie regression). Core fix: `getCompleteTvProviderCountries` now folds the same episode fan-out twice (per-season, then across seasons) instead of intersecting season granularity away. Per-season availability needs `EXPO_PUBLIC_TMDB_TV_EPISODE_LOOKUP=true`; the sheet degrades to a fallback line when off |
| 2026-07-24 | Results / ResultView | Synopsis expand/collapse ("read more") — manual `substring(0,250)` truncation, whole paragraph as invisible toggle | Letterboxd film-page synopsis "…more" clamp (Apple TV detail-page "…MORE" as big-app analog) | A-tier data plumbing wrapped around a C− interaction that looks broken on every long synopsis | Approved | All 4 implemented and verified on release APK on the A54 (device R5CW31BDEMH). (1) `numberOfLines={4}` line clamp replaces the `substring(0,250)` slice — native ellipsis breaks at the column edge, no more mid-word cut; (2) gold "Read more / Read less" toggle gated on measured overflow — the first paint renders unclamped so `onTextLayout` counts true lines, then clamps (flash happens offscreen below the 480px hero); (3) a11y — the overflow branch is a real `accessibilityRole="button"` carrying the full synopsis as its `accessibilityLabel` + `accessibilityState={{ expanded }}`, so TalkBack no longer reads the truncated fragment; (4) tap-anywhere retained atop the visible link. Verified round-trip on The Hangover (clamped→Read more→full text ending cleanly at "church on time."→Read less→re-clamped); short synopses show no toggle. 154/154 tests green, 0 lint errors. Not committed — changes sit uncommitted on branch `fix/share-card-country-picker`. a11y props code-confirmed on the now-live button branch; couldn't `uiautomator dump` (the mesh-orb loop blocks idle state). |
| 2026-07-23 | Results / ResultView | Share Card flow (share button → country picker sheet → off-screen ViewShot capture → OS share) | Spotify share sheet on a track/album (live card preview, per-destination formats, live `spotify.link`) | B for the artifact, D for the flow, F for the payoff — a great card, generated blind, timed by a stopwatch, carrying a QR code to nowhere | Approved | All 7 implemented and verified on release APK on the A54. QR payload is now the TMDb title URL (user's call); this forced a QR encoder bump from version 2-L to 3-L, since v2 held 32 bytes and `qrMatrix.js` silently truncated — a 40-byte TMDb URL would have scanned to a broken address. New `tests/share-card.test.js` decodes the matrix back the way a scanner does (round-trip proof), 151/151 suite green. Deep links needed `getTitleMatchById` (a bare `{tmdbId, mediaType}` rendered a detail screen titled "undefined") plus a ref-held callback in `useDeepLink` — `openResolvedDetail`'s identity changes when `recentViewed` loads, which was tearing down the in-flight handler and dropping ~1 in 3 cold starts; now 4/4. Three bugs surfaced only on device: the story cap desynced `imageUris` from rendered images so readiness never armed; React reconciles the two card layouts positionally and reuses loaded `<Image>`s (fixed with `key={format}`); and the preview measured its height inside the constrained container, cropping instead of scaling. Not exercised on device: the disabled-CTA empty-selection state (logic unit-tested only) |
