# Feature Critique — ledger

One row per critique run. Read this before picking a target (skip anything listed
here unless the user points at it on purpose); append to it at the end of every
run, including rejected critiques.

| Date | View | Feature | Compared to | Verdict | Decision | Outcome |
|------|------|---------|-------------|---------|----------|---------|
| 2026-07-23 | Results / ResultView | Bookmark → save to collections | Spotify add-to-playlist sheet | B− flow wearing an A-tier feature set | Adjusted — "user must pick a destination every time" | Shipped — PR #68 |
| 2026-07-23 | Results / ResultView | Where To Watch availability table | JustWatch title-detail "Where to watch" | C+ presentation wrapped around an A− dataset | Adjusted — global atlas is the point (don't localise it away); no deep links (users just want to know which country has it); free-to-watch only is deliberate, rent/buy stays excluded | All 8 shipped — PR #70, verified on release APK on the A54 |
| 2026-07-23 | Results / ResultView | Cast & Crew rail (grouping, See All overflow, long-press peek sheet) | IMDb title page "Top cast" + "Cast & crew" full page | A-tier cards on a rail with no information architecture — B for any single card, D for everything past card ten | Approved | All 8 shipped — PR #71, verified on release APK on the A54; user chose full-cast screen for #2 and left #5's affordance to me (persistent corner badge) |
| 2026-07-23 | Results / ResultView | Seasons rail (TV titles) — inert cards, no header, no season-scoped availability | Apple TV app series page (season selector + per-season provider attribution) | A-tier data pipeline feeding a rail of coasters — B+ for the fetch, D for the section | Approved | All 7 shipped — verified on release APK on the A54 (Squid Game 3-season, Chernobyl 1-season, Parasite movie regression). Core fix: `getCompleteTvProviderCountries` now folds the same episode fan-out twice (per-season, then across seasons) instead of intersecting season granularity away. Per-season availability needs `EXPO_PUBLIC_TMDB_TV_EPISODE_LOOKUP=true`; the sheet degrades to a fallback line when off |
| 2026-07-24 | Results / ResultView | Synopsis expand/collapse ("read more") — manual `substring(0,250)` truncation, whole paragraph as invisible toggle | Letterboxd film-page synopsis "…more" clamp (Apple TV detail-page "…MORE" as big-app analog) | A-tier data plumbing wrapped around a C− interaction that looks broken on every long synopsis | Approved | All 4 implemented and verified on release APK on the A54 (device R5CW31BDEMH). (1) `numberOfLines={4}` line clamp replaces the `substring(0,250)` slice — native ellipsis breaks at the column edge, no more mid-word cut; (2) gold "Read more / Read less" toggle gated on measured overflow — the first paint renders unclamped so `onTextLayout` counts true lines, then clamps (flash happens offscreen below the 480px hero); (3) a11y — the overflow branch is a real `accessibilityRole="button"` carrying the full synopsis as its `accessibilityLabel` + `accessibilityState={{ expanded }}`, so TalkBack no longer reads the truncated fragment; (4) tap-anywhere retained atop the visible link. Verified round-trip on The Hangover (clamped→Read more→full text ending cleanly at "church on time."→Read less→re-clamped); short synopses show no toggle. 154/154 tests green, 0 lint errors. Not committed — changes sit uncommitted on branch `fix/share-card-country-picker`. a11y props code-confirmed on the now-live button branch; couldn't `uiautomator dump` (the mesh-orb loop blocks idle state). |
| 2026-07-23 | Results / ResultView | Share Card flow (share button → country picker sheet → off-screen ViewShot capture → OS share) | Spotify share sheet on a track/album (live card preview, per-destination formats, live `spotify.link`) | B for the artifact, D for the flow, F for the payoff — a great card, generated blind, timed by a stopwatch, carrying a QR code to nowhere | Approved | All 7 implemented and verified on release APK on the A54. QR payload is now the TMDb title URL (user's call); this forced a QR encoder bump from version 2-L to 3-L, since v2 held 32 bytes and `qrMatrix.js` silently truncated — a 40-byte TMDb URL would have scanned to a broken address. New `tests/share-card.test.js` decodes the matrix back the way a scanner does (round-trip proof), 151/151 suite green. Deep links needed `getTitleMatchById` (a bare `{tmdbId, mediaType}` rendered a detail screen titled "undefined") plus a ref-held callback in `useDeepLink` — `openResolvedDetail`'s identity changes when `recentViewed` loads, which was tearing down the in-flight handler and dropping ~1 in 3 cold starts; now 4/4. Three bugs surfaced only on device: the story cap desynced `imageUris` from rendered images so readiness never armed; React reconciles the two card layouts positionally and reuses loaded `<Image>`s (fixed with `key={format}`); and the preview measured its height inside the constrained container, cropping instead of scaling. Not exercised on device: the disabled-CTA empty-selection state (logic unit-tested only) |
| 2026-07-24 | Results / ResultView | Hero content title — fixed 56px, no `numberOfLines`/auto-shrink, bottom-anchored stack grows upward on long titles | Apple TV app detail-page title treatment (logo-art-first + scaling text fallback); Netflix logo-art as corroboration | A-tier typography with zero fault tolerance — a magazine cover that assumes every title is one word | Approved | Recommendations delivered (4, prioritized). Core finding: the uncapped bottom-anchored title ([ResultView.js:1302](src/components/ResultView.js:1302), heroContent [:2305]) is the ROOT CAUSE of the "Metacritic badge under the floating buttons on long titles" overlap that the ratings-strip run deferred — fixing title growth retires it for free. Recs: (1) `numberOfLines={3}` + reserved band / floor above the floating header; (2) `adjustsFontSizeToFit` minScale ~0.6 (release-APK verify, Android+custom-font flaky — measured-length tier as fallback); (3) neutralize `letterSpacing:-2` glyph-clip via token relax and/or `alignSelf:'stretch'`+`textAlign` per [[custom-font-truncation-rescope]]; (4) title-treatment logo art (TMDb `images.logos`, not fetched today) flagged as SEPARATE scope. First-pass scope = #1+#2+#3. **All 4 implemented and verified on release APK on the A54 (R5CW31BDEMH), branch `fix/ratings-strip-fidelity` (uncommitted).** #4: `getTitleMetadata` now appends `images` with `include_image_language=en,null`; new `pickTitleLogo` picks best raster (non-SVG, en>null>other, then votes) → `result.titleLogo {url, aspectRatio}`; ResultView renders logo-first (`ExpoImage`, contain/left, sized into a verticalScale(84) band, `onError`→text fallback). D&D shows the official title logo. #1: `heroContent` gained a `top` floor (`insets.top+52`) + `justifyContent:'flex-end'` + `overflow:'hidden'` so the bottom-anchored stack can't draw over the floating share/bookmark buttons — retires the deferred ratings-strip overlap. #2: deterministic length tiers (≤16→56px, ≤40→34px, else 28px) replace `adjustsFontSizeToFit` — device test proved `numberOfLines={3}` alone doesn't bound *height*, so 3 full-size lines clipped the whole meta stack; tiers shrink long titles (D&D→34px/2 lines) so the ratings strip survives. #3: `letterSpacing:-2`→`-1` + `alignSelf:'stretch'`+`textAlign:'left'` — no last-glyph clip on "Thieves"/"Moon"/"Barbie". Verified 4 cases: D&D logo path (clean); D&D forced-text (34px, full title, meta intact); Killers of the Flower Moon (soundtrack+trailer worst case — ratings strip preserved, no clip); Barbie (short, full 56px). 160/160 tests, 0 lint errors. Meta-stack margins tightened (marginBottom 20→12, gap 12→10) to keep ratings clear even with a soundtrack button. Spotted but OUT OF SCOPE: Killers' soundtrack button renders a raw Wikidata QID ("Q123137009") instead of a track name — pre-existing soundtrack-feature bug |
| 2026-07-24 | Results / ResultView | Hero artwork selection — which image sits behind the title treatment; `backdropUrl \|\| posterUrl` takes TMDb's community-voted primary, double-titling now that we render our own logo/text | Apple TV / Netflix "background art vs title art" split (textless background mandated so the overlaid title never collides) | Picks the wrong *kind* of art, not the wrong language — TMDb's crowd-pleaser key-art is the one image guaranteed to fight a title treatment | Approved (after user supplied LOTR + Portrait examples that disproved the first-pass "filter iso_639_1:null" fix) | Recommendations delivered (4, prioritized). Diagnosis corrected mid-gate via device evidence: deep-linked LOTR Fellowship (id 120) + Portrait of a Lady on Fire (id 531428) on the release APK — LOTR's on-screen backdrop IS `iso_639_1:null` yet is ornate Elvish-script key-art, proving TMDb's `null` tag = "no English *title* text", NOT "clean still". Two failure modes: (1) literal double-title — poster fallback (poster always has title; ~11% of low-popularity tail has NO backdrop → poster) + mistagged backdrops; (2) key-art clutter — TMDb community upvotes dramatic promo art as `backdrop_path` primary, which is exactly what `heroArtUri` grabs ([ResultView.js:1040](src/components/ResultView.js:1040)). Measured: 50 popular movies + 44 popular TV = 0 with-text/0 no-backdrop (mainstream already clean); problem is entirely the obscure/foreign/TV tail. Recs: (1) `pickHeroBackdrop(images.backdrops)` selector mirroring `pickTitleLogo` — `null`-only, prefer ~1.778 aspect + resolution, do NOT rank by votes (votes surface the key-art), fallback to old `backdrop_path`; honest ceiling — heuristic, TMDb has no "clean still" flag. (2) Suppress our title overlay when the hero falls back to the poster ([ResultView.js:1206-1209](src/components/ResultView.js:1206)) — deterministic double-title kill. (3) Title-legibility scrim behind the logo/text (extend the existing bottom scrim [:1179-1186]) — robust catch-all independent of TMDb data quality. (4) Prereq: unify where `backdropUrl` is set (search [tmdb.js:262/:317], `getTitleMatchById`, cached recentViewed) — found the on-device Portrait backdrop ≠ detail-endpoint `backdrop_path`, so selector must live where all paths converge (`getTitleMetadata`/`resolveMatch`). `images` already fetched for logos, so selection is free. First-pass scope = #1+#2+#3; verify on LOTR/Portrait/poster-fallback/2 mainstream. **Implemented + verified on release APK on the A54 (R5CW31BDEMH), branch `fix/ratings-strip-fidelity` (uncommitted).** #1 `pickHeroBackdrop` in tmdb.js → `result.heroBackdropUrl`, consumed first in ResultView (`heroBackdropUrl || backdropUrl || posterUrl`). KEY LESSON from device: the selector must rank the textless (`iso_639_1==null`) pool by **community vote_count** (with a ~16:9 preference, resolution tiebreak), NOT by aspect/resolution alone — a first cut ranking width-first regressed D&D to a dark still; votes are the hero-quality signal, and the ornate key-art that fights the title (LOTR ring inscription) is TMDb's designated *primary* but NOT top-voted-null, so votes both keep great defaults (D&D warm cast shot) and step around the key-art. #2 title overlay (logo/text) suppressed when `usingPoster` (no backdrop of any kind) so a title-bearing poster isn't double-titled. #4 selector lives in `getTitleMetadata` where all resolveMatch paths converge. **#3 (title-legibility scrim) BUILT THEN REMOVED**: first impl was a `StyleSheet.absoluteFill` LinearGradient INSIDE the inset `heroContent` (left/right:24, overflow:hidden) → rendered a hard-edged dark rectangle with a visible vertical "cut-off" line, glaring on Barbie's bright sky; user flagged it. Removed — #1's clean-still selection makes it redundant and logos/text stay legible without it (verified LOTR gold logo + Portrait white handwritten logo on their stills). Verified 5 heroes on device: LOTR (ring-art→clean hobbits still), Portrait (→clean beach still), D&D (→warm cast still, no dark regression, no line), Barbie (→bright still, no dark box), Vampire Princess Miyu id 31649 (no backdrop → poster shown, our title suppressed, no double-title). 160/160 tests, 0 lint errors |
| 2026-07-25 | Results / ResultView | Watch Trailer — trailer selection (`getTitleMetadata`), the `TrailerModal` popup, auto-rotate, and age-restricted playback | YouTube Android app's watch-page player (rotate-to-fullscreen, typed error surfaces, signed-in age gate) | A correctly-plumbed YouTube embed with every failure path amputated — A− on the happy path, F on everything else | Approved | Recommendations delivered (6, prioritized). **Measured, not assumed** — 120 popular titles (movie+TV pages 1–3) run through the real picker ([tmdb.js:470-474](src/lib/tmdb.js:470)), each picked YouTube key probed for `playabilityStatus`: 29/120 (24%) render NO button; of the 91 with a button, 66 official Trailer / 19 unofficial / **6 not a trailer at all** (Mentalist→"Behind the Scenes: The Designers", SVU→featurette, Letterman→opening credits) and **4/91 (4.4%) unplayable in an embed** — Vixen! `FuMIkgbiyGI` = `LOGIN_REQUIRED` "Sign in to confirm your age" (the live age-gate case), Deep In `HBwTZENixTE` geo-blocked, Tonight Show `zyl4Vvjc7pI` copyright-removed, Sparrow's Fluttering content-claimed. **None fire `onError`** — the WebView loads HTTP 200 and YouTube renders its own error inside, so the spinner clears ([:163](src/components/TrailerModal.js:163)) over a permanent black rectangle. Opening hypothesis CORRECTED by data: `language=en-US` does NOT starve popular foreign titles (Parasite/Squid Game/AoT all resolve, though pools shrink 18→4); the damage is entirely in the tail — 8 of the 29 buttonless titles are rescued purely by adding `include_video_language`. Rotation finding: NOT fully broken — `react-native-webview` sets `SCREEN_ORIENTATION_UNSPECIFIED` on fullscreen ([RNCWebViewManagerImpl.kt:160](node_modules/react-native-webview/android/src/main/java/com/reactnativecommunity/webview/RNCWebViewManagerImpl.kt:160)), overriding the manifest portrait lock at runtime; but it's reachable ONLY via YouTube's own tiny fullscreen icon, and the modal itself never rotates. Recs: (1) load an IFrame-Player-API host page (`source={{html, baseUrl:'https://www.youtube.com'}}`) instead of the embed URI so error codes 101/150/100/2/5 become visible — the prerequisite for everything else; keep the hand-tuned UA [:20] + `Referer` [:151] and fall back to the direct URI if the bridge doesn't `onReady`; (2) restore the DEAD `errorBox`/`errorText`/`errorAction` styles ([:259-278](src/components/TrailerModal.js:259), defined and never rendered) and delete the toast-and-auto-close ([:169](src/components/TrailerModal.js:169), [:82-87](src/components/TrailerModal.js:82)) — age-gate copy hands off to YouTube via existing `handleOpenYouTube` [:89] (an embed can NEVER play age-restricted; handoff is the only honest fix); (3) picker — add `include_video_language`, replace the `youtubeVideos[0]` fallthrough with tiers (official Trailer→official Teaser→any Trailer→any Teaser→nothing), carry `trailerType` so the button can say "Watch Teaser", and (3d, depends on #1) return the ranked list so a dead video auto-advances; (4) `expo-screen-orientation` (not a dep today) — `unlockAsync()` NOT `OrientationLock.ALL` so the system auto-rotate toggle is honored; restore in cleanup not just `handleClose`; **ordering trap**: unlock BEFORE the WebView mounts or its `initialRequestedOrientation` snapshot ([:146](node_modules/react-native-webview/android/src/main/java/com/reactnativecommunity/webview/RNCWebViewManagerImpl.kt:146)) re-locks portrait on fullscreen exit; manifest already has `configChanges=orientation\|screenSize` so no activity recreate; leave `app.json:7` portrait; (5) look & feel — force dark chrome (`colors.surface` is `#ffffff` in light mode → white header bar + full-screen white flash), own the StatusBar (dark icons on the `#000` inset strip make the clock/battery vanish), 16:9 frame instead of `flex:1` letterboxing into a black void, move the YouTube pill into the header, swipe-to-dismiss, `fade` not `slide`; (6) `heroBackdropUrl` as poster frame behind the loader. Credit due: the Android UA spoof + `Referer` + "don't add `origin=`" comment are exactly right and why 87/91 play flawlessly. First-pass scope = #1+#2+#3+#4 (+#5 rides along). **ALL 6 IMPLEMENTED and verified on the release APK on the A54 (R5CW31BDEMH), branch `feat/detail-hero-polish` (uncommitted); APK archived as `releases/trova-1.0.7-release-2026-07-25.apk`. 168/168 tests (new `tests/trailer-picker.test.js`, 8 cases), 0 lint errors/0 warnings in the touched files.** New `src/lib/trailerPicker.js` holds `rankTrailerCandidates` (pure, unit-tested): tiers official Trailer→official Teaser→any Trailer→any Teaser→**nothing**, then English→null→other, then newest `published_at`, capped at 5 candidates; `tmdb.js` gained `include_video_language: 'en,null'` on the main call plus a one-extra-request original-language sweep fired only when the tiered pick is empty, and returns `trailerType` + `trailerCandidates`. **KEY LESSON — Rec #1 as specified was WRONG and device testing caught it**: serving an IFrame-Player-API host page via `{html, baseUrl:'https://www.youtube.com'}` does NOT give the document a real youtube.com origin on Android (`loadDataWithBaseURL` leaves it opaque), so YouTube answered **every** video with error 152 — the exact trap the original `origin=` comment warned about. Proof: instrumented the error code, then all five Blade Runner 2049 candidates failed 152 in sequence. Vixen! showing an error state on the first build was a FALSE POSITIVE (right UI, wrong reason). Fix: keep the proven direct `/embed/` URI as the document and observe the player *from inside it* via `injectedJavaScript` — same-origin, so it polls `.ytp-error` (refusal) and `movie_player.getPlayerState()` 1/3 (picture on screen). Reading YouTube's own refusal **text** beats the numeric codes: it separates an age gate from a region block, which 101/150 collapse together. Device-verified: Vixen! → text "This video is age-restricted and only available on YouTube" → age branch ("This trailer is age-restricted… Open it in YouTube, where you're signed in") + gold CTA; FROM → candidate #1 `78tTuVoKONg` geo-blocked ("uploader has not made this video available in your country") → **auto-advanced silently to #2, which played** (user saw no error); The Mentalist → button GONE (its featurette no longer masquerades as a trailer); Jana Nayagan (ta) and Kung Fu Soccer (zh) → button now present and playing; Blade Runner 2049 + Arcane → play clean, no refusals. Rotation: `expo-screen-orientation` added; `unlockAsync()` (NOT `OrientationLock.ALL`, which would override a user who turned auto-rotate off) before the WebView mounts, `lockAsync(PORTRAIT_UP)` in the effect cleanup. Device-verified with auto-rotate OFF: forcing `user_rotation 1` rotated the app mid-playback (`cur=2340x1080`, header dropped, floating close top-left, no reload thanks to the existing `configChanges`), and backing out restored `cur=1080x2340` even with `user_rotation` still 1. Chrome: hardcoded player palette — light mode verified showing a dark header and no white flash (was `colors.surface`=`#ffffff`). Also: 16:9 `aspectRatio` stage replaces `flex:1` letterboxing, YouTube pill moved into the header, `heroBackdropUrl` poster behind the loader, swipe-down dismiss via plain responder props (a `PanResponder` tripped the `react-hooks` refs-during-render rule), `fade` not `slide`, dead `errorBox` styles finally rendered, toast-and-auto-close deleted. **Re-measured the original 120-title sample against the new picker**: non-trailer buttons 6→**0**; 7 titles rescued by `include_video_language`; 1 saved by auto-advance; 4 genuinely unplayable now get an explained error state instead of a black rectangle. Net "no button" 29→28 — composition changed, not just the count: +7 real trailers gained, ~6 lying buttons removed. A `console.warn` on refusal was kept deliberately — it's the instrumentation that caught the 152 bug and the failure class is otherwise invisible. Not verified on device: the no-candidates-at-all empty state (no sampled title reached it), and the status bar stays visible in landscape despite `hidden={isLandscape}` (cosmetic, left as-is). **FOLLOW-UP ROUND — user reported 3 defects after first delivery, all fixed and re-verified.** (a) *Close and YouTube buttons felt unresponsive* — self-inflicted: the header's `onMoveShouldSetResponder={() => touches.length === 1}` claimed the responder on ANY movement, so a tap with a pixel of finger drift stole it from the child buttons and cancelled the press. Fixed by recording the touch origin in `onStartShouldSetResponderCapture` (returns false, never claims) and only claiming on `dy > 15 && dy > |dx|`. Device-verified: close dismissed within 900ms of tap; YouTube pill had `com.google.android.youtube` topResumedActivity within 1.2s. (b) *Landscape didn't fit the screen* — root cause found by instrumenting both sides: RN was correct (`frame layout 832 384`, stage `{x:0,y:0,w:832,h:384}`) but the WebView reported a **909x420dp viewport inside an 832x384dp view** — it caches the display density it was created with and MainActivity's `configChanges` (the very thing that keeps playback alive) means the activity never recreates to refresh it, so the page lays out at ~412dpi instead of the A54's 450 and YouTube sized `#movie_player` to 832 *CSS* px offset by 29. Fix: inject a stylesheet pinning `html,body,#movie_player,.html5-video-player` to 100%/left:0/top:0 so the miscomputed pixel size stops mattering. (c) Also added a `useLayout`-measured `frame` (onLayout on the modal container) because `useWindowDimensions` reports the *activity* window (755.55dp) not the modal's (832dp). **Two attempted fixes were built, measured, and REVERTED as provably useless**: `movie_player.setSize(innerWidth, innerHeight)` and remounting the WebView keyed on orientation (with `start=` resume) — screenshots at each step were geometrically identical to the CSS-only fix, so the remount was removed and playback again survives rotation with no reload. **Also corrected a false alarm**: the remaining 210px-per-side black bars in landscape are CORRECT — Arcane/Blade Runner trailers are 16:9 uploads (scope image with bars baked in), so 1920x1080 centred in a 2340x1080 screen is exactly right; I had briefly mistaken this for residual mis-sizing. Gotcha worth remembering: `PLAYER_OBSERVER_JS` is a template literal, so a backtick in a comment inside it (I wrote \`configChanges\`) silently breaks the Metro bundle with a parse error at build time, not lint time |
| 2026-07-25 | Results / ResultView | Spotify soundtrack CTA — gold button under Watch Trailer, `Choose Soundtrack (N)` picker sheet, Wikidata `P406→P2205` → Spotify handoff | Google movie knowledge-panel soundtrack module (a non-music product surfacing a film's music from a third-party knowledge graph — same job, same failure class) | A-tier plumbing wired to a button that can't say what it does — B+ for the handoff, D for the label, F for the 17.5% that ship a database key as a call to action | Approved | Recommendations delivered (5, prioritized), no code written. **Measured live against Wikidata, not assumed** — ran the app's own query shape ([ResultView.js:237](src/components/ResultView.js:237)): 953 bindings → **800 titles worldwide** can ever render this button (750 single / 50 multi), 871 releases. Defects quantified: **140/800 titles (17.5%) render a gold CTA whose entire text is a bare QID** — `parseSoundtracksFromBindings` guards against the entity *URI* form ([wikidataSoundtracks.js:133](src/lib/wikidataSoundtracks.js:133)) but `wikibase:label` returns a bare `"Q123137009"` string for unlabelled entities, which sails through; confirmed on the ledger's own example `tt5537002` (Killers of the Flower Moon) → `soundtrackLabel = "Q123137009"`. **862/871 releases (99.0%) have no `P18` cover**, so the picker's 56px art column is a musical-note glyph by default, not by fallback ([SoundtrackPickerSheet.js:43](src/components/SoundtrackPickerSheet.js:43)); and **64% of multi-release sheets share one year across every row** (Frankenweenie both 2012, Samurai Champloo all four 2004, 8 Mile both 2002), so the only disambiguator is inert. Root cause named: using the Wikidata album title *as* the button text ([ResultView.js:1427](src/components/ResultView.js:1427)) — every other defect is downstream (QID leak is only visible because the label is the CTA; truncation only matters because a 63-char *"Mad Max Beyond Thunderdome – Original Motion Picture Soundtrack"* is load-bearing in a `numberOfLines={1}` pill; the verb-less gold twin of Watch Trailer only exists because the entity took the action's slot — both use `styles.trailerButton` + `GOLD_ACCENT`). Recs: (1) fixed action label ("Soundtrack"/"Play Soundtrack"), album demoted to sheet content — recommended variant is *always* open the sheet incl. single-release (shows which album before throwing the user into Spotify, collapses two paths, matches [[save-flow-pick-every-time]]); also de-emphasize from primary since two full-width gold primaries stacked is one too many; (2) `/^Q\d+$/` reject in the shared parser — **keep** QID-labelled releases (they're playable, just nameless) and label the row "Untitled release" rather than dropping 140 titles; put the check in the parser not the render site since `basedOn` [:289](src/components/ResultView.js:289), awards, languages and countries all use the same `isEntityUri` idiom and share the exposure (leak rate on those paths NOT measured — worth a look); (3) route covers through the existing `resolveCommonsThumbUrls(values, 250)` ([:54](src/lib/wikidataSoundtracks.js:54)) which is exported, unit-tested and already used by awards ([wikidataAwards.js:124](src/lib/wikidataAwards.js:124)) — but capped payoff, only 9/871 releases (1.0%) have a cover at all; (4) picker rows should lead with the distinguishing fragment (strip the redundant leading film title, drop the year line when all rows share it) — deferred, only pays off on 50 titles, and a second SPARQL arm for track count/artist doesn't earn its keep; (5) delete the `wikiLoading` skeleton pill ([:1405-1414](src/components/ResultView.js:1405)) — it reserves button-shaped space on *every* detail screen and collapses on ~99.9% of them; leave the awards skeleton [:2026](src/components/ResultView.js:2026) alone, its odds are far better. **Withdrew critique point #6 (search fallback) at recommendation time**: the `spotifyAlbumId` hard gate ([:130](src/lib/wikidataSoundtracks.js:130)) is the same discipline the trailer feature had to be retrofitted with after shipping 4.4% unplayable buttons — a `?q=<title> soundtrack` link would put a gold CTA on every title while guaranteeing nothing on the other end. Credit due: `openSpotifyAlbum` ([spotify.js:6-26](src/lib/spotify.js:6)) is exemplary (native scheme → `canOpenURL` guard → try/catch → web player, no dead ends); dedupe-by-QID is load-bearing (953→871); the feature is free, riding the Wikidata call already in flight for `basedOn`/awards; and no mainstream streaming-finder links a film to its soundtrack album at all. Suggested first pass = #1+#2 | **ALL 5 IMPLEMENTED and verified on the release APK on the A54 (R5CW31BDEMH), branch `feat/trailer-playback` (uncommitted); APK archived as `releases/trova-1.0.8-release-2026-07-25.apk`. 179/179 tests (11 new in `tests/wikidata-soundtracks.test.js`), 0 lint errors and no new warnings (46 pre-existing mesh-orb `react-hooks/refs` warnings, identical count at HEAD).** User took both open calls my way: #1 always opens the sheet, #2 keeps QID-labelled releases as "Untitled release". New pure helpers in `wikidataSoundtracks.js`: `isBareQid`, `labelOrNull`, `stripLeadingTitle`, `buildSoundtrackRows`, `resolveSoundtrackCovers`. Button is now a fixed "Soundtrack" / "Soundtrack (N)" in a new `soundtrackButton` style — gold tonal outline (`GOLD_DIM` border, 10% gold fill, `GOLD_ACCENT` text/icon) instead of a second solid-gold primary competing with Watch Trailer. `parseSoundtracksFromBindings` keeps unlabelled-but-playable releases with `title: null`; `sortSoundtracks` made null-safe and sorts unnamed last. **GAP CAUGHT DURING IMPLEMENTATION, not in the recommendations**: watchlist entries enriched by an older build still hold a bare QID in AsyncStorage and reach the UI via the cached branch ([ResultView.js:517-524](src/components/ResultView.js:517)) *without* being re-parsed, so the parser-level guard alone would still have leaked QIDs for saved titles — `buildSoundtrackRows` therefore re-runs `labelOrNull` at presentation time (covered by its own test). Device-verified: **Killers of the Flower Moon → "Soundtrack" button, sheet row "Untitled release / 2023"** (was a gold CTA reading "Q123137009"); tapping through opened `https://open.spotify.com/album/43MjCtL4DwTxc1DhXK9Kpp` — exactly the id Wikidata holds — via the web-player fallback, since Spotify isn't installed on this device, confirming `openSpotifyAlbum`'s native→web path intact. **Frankenweenie (62214) → "Soundtrack (2)" → rows "Original Motion Picture Soundtrack" + "Unleashed! – Music Inspired by the Motion Picture", no year lines** (both 2012, uniform → hidden). **Yellow Submarine (12105) → rows "Yellow Submarine / 1969" + "Songtrack / 1999"** — years shown because they differ, and row 1 keeps its full name because stripping would empty it. Both branches of the year rule and both branches of `stripLeadingTitle` verified on device. **Blade Runner 2049 (335984) mid-load: no shimmer bar between Watch Trailer and the info pills at 3s/5s/13s** — #5 confirmed, layout stable from first paint. #3 (thumbnail resolver) is unit-tested only: none of the device titles carry `P18` art, consistent with the measured 99.0% — not visually confirmed, and by design it can only ever affect ~1% of releases. Regression-checked the sibling label paths I touched: a live replay of the new `labelOrNull` handling over 6 adaptations (LOTR Fellowship, Requiem, KOTFM, The Dark Knight, Breaking Bad, Barbie) dropped **0** rows, and Requiem for a Dream renders "BASED ON — Literary work: Requiem for a Dream by Hubert Selby Jr." plus intact Cast/Crew/Awards on device. Two gotchas for next time: the app's package is **`kkadogo.findstreamer.com`**, NOT `com.jeanm.trova` — force-stopping the wrong name silently no-ops and leaves a stale sheet mounted over a deep-linked screen (this briefly looked like a stale-content bug and was purely a test artifact); and `uiautomator dump` still fails here (mesh-orb loop blocks idle), so slow `input swipe … 900` drags are needed instead of default flings, which overshoot whole sections | Shipped — all 5 verified on device (uncommitted) |
| 2026-07-25 | Results / ResultView | Bottom recommendation rails — "More Like This" (`getSimilar`) and "More From Cast & Crew" (`getMoreFromCastAndCrew`) | Netflix "More Like This" title-page row (Apple TV person-scoped "More From <name>" rails as corroboration on attribution) | A rail that answers "what next" with a 1-vote film, and a rail that answers "who else" without saying who — B for the plumbing, D for the ranking, F for the labelling | Approved | Recommendations delivered (6, prioritized), no code written. **Measured against live TMDb via the app's own logic** — 100 popular titles (60 movie pages 1–3 / 40 TV pages 1–2), plus a 5-title latency run. *More Like This*: `/similar` and `/recommendations` overlap **1.0%** — effectively disjoint datasets, and the app picked the keyword one while [`fetchSurpriseRecommendation`](src/lib/tmdb.js:743) in the same file already calls `/recommendations`. The `vote_average` re-sort at [tmdb.js:726](src/lib/tmdb.js:726) has **no vote-count floor**: **51.8% of shown picks have under 200 votes**, median vote count of an on-screen card is **168**, and the 5 cards sit on average **9.2nd** in TMDb's own relevance order (the sort actively discards the most-similar items). Real output — The Odyssey → *LANDEaD* **10.0 from 1 vote**; Moana → *Les Franglaises – Le Viens-Retour* **9.0 from 2 votes**. Also **15/450 picks have no poster** and still render (sibling function filters this at [:1409](src/lib/tmdb.js:1409)); 10/100 titles return an empty `/similar`. *More From Cast & Crew*: `with_people=a\|b\|c` OR-join ([:1400](src/lib/tmdb.js:1400)) + `vote_average.desc` + `vote_count.gte:100` is a blockbuster magnet — **Avengers: Infinity War appears on 11 of 100 title pages**, Endgame 9, The Dark Knight 5, Across the Spider-Verse 5 — and the connection is structurally invisible (*Backrooms* → **12 Years a Slave** via Chiwetel Ejiofor; *Minions & Monsters* → **Inglourious Basterds**, **The Help**). `/discover/movie` is hardcoded at [:1399](src/lib/tmdb.js:1399) so **33/40 TV pages render a movies-only rail** under an unqualified header. Both rails gate first paint — `getSimilar` is a serial await ahead of `getProviderCountries` ([tmdb.js:1490-1491](src/lib/tmdb.js:1490)) and the cast/crew rail fires **26 requests per detail screen** (credits + discover + 12 `external_ids` + 12 OMDb via [`enrichDiscoverResults`](src/lib/tmdb.js:1075)), measured **~800ms for the TMDb leg alone**, OMDb on top. Render defects: both rails share `styles.similarRating` [:2859](src/components/ResultView.js:2859) but it means IMDb on one ([:2208-2214](src/components/ResultView.js:2208), hardcoded `#F5C518` + inline `fontSize:10` bypassing typography tokens) and a bare unlabelled number on the other ([:2256-2260](src/components/ResultView.js:2256)); neither shows a year; a11y label is `Open details for ${title}` on both. Recs: (1) vote-count floor + drop the rating re-sort in `getSimilar` (reuse the `voteCount >= 75` shape already at [tmdb.js:779](src/lib/tmdb.js:779); `vote_count` isn't currently captured in the map); (2) `/similar` → `/recommendations`, lands in the same 3 lines as #1; (3) **replace `/discover/movie` with per-person `/person/{id}/combined_credits`** for the top 3–4 people — attribution becomes free (the OR-join structurally cannot say which person matched), kills the blockbuster attractor, returns TV so the movies-on-TV-pages bug retires for free, and cuts 26 requests to 4; knowingly loses the IMDb re-sort at [:1424-1430](src/lib/tmdb.js:1424) (re-add on the final 5 only if wanted, 5 requests not 24); (4) defer both rails off the critical path, mirroring the existing Wikidata post-paint + skeleton + retry pattern in this same component ([ResultView.js:631](src/components/ResultView.js:631), [:2045](src/components/ResultView.js:2045)); (5) extract one shared rail card with year + source-labelled score reusing `ScoreValue` [:369](src/components/ResultView.js:369) — the franchise rail [:1859-1875](src/components/ResultView.js:1859) is already the target pattern in-file; (6) `poster_path` filter + re-measure the empty state after #2. Credit due: **zero cross-rail duplicates across 100 titles** (measured); the OMDb IMDb re-sort is the right instinct in the wrong place; the cast/crew rail *does* filter posterless (the asymmetry is the bug, not this half); `onSelectSimilar` wired identically across all 4 nav stacks with correct `mediaType` passthrough; and the OR-join genuinely lands when the person pool is director-dominated (The Odyssey → The Dark Knight, Interstellar, Inception). **Open decision left with the user**: one merged rail with per-card attribution (my default, less layout work) vs Apple-TV-style one rail per person. Suggested first pass = #1+#2, then #3+#4 together | **ALL 6 IMPLEMENTED and verified on the release APK on the A54 (R5CW31BDEMH); APK archived as `releases/trova-1.0.9-release-2026-07-25.apk`. 195/195 tests (16 new in `tests/rail-picker.test.js`), 0 lint errors; ResultView warnings 46→47, the one addition being `react-hooks/set-state-in-effect` in the new rails effect — the identical pattern the existing Wikidata effect already uses (HEAD baseline confirmed by linting `git show HEAD:` output).** I took both open calls my way (merged rail with per-card attribution; OMDb re-sort dropped). New pure module `src/lib/railPicker.js` (`rankSimilarTitles`, `selectRailPeople`, `creditsForPerson`, `rankPeopleTitles`, `railScore`) + new `src/components/TitleRailCard.js`; `fetchTitleRails` in tmdb.js is the single post-paint entry point. **Re-measured the SAME 100-title sample through the shipped module: cards under 200 votes 51.8% → 7.6%; cards under 50 votes → 0; median vote count of an on-screen card 168 → 781; empty More Like This rails 10 → 5; cast/crew cards carrying attribution 0% → 100%; TV pages whose people rail includes at least one series 0/40 → 23/40; cross-rail duplicates 0 → 0.** TWO THINGS THE RECOMMENDATIONS GOT WRONG, both caught by measuring rather than reasoning: (1) *backfill was underspecified* — "backfill in relevance order" let the one-vote LANDEaD back onto the rail at position 4, which defeats the entire fix; corrected to a two-tier floor (`MIN_RAIL_VOTES` 200 preferred, `ABSOLUTE_MIN_RAIL_VOTES` 50 hard) plus `MIN_RAIL_ITEMS` 2 so a rail hides rather than showing a single stub card. (2) **per-person credits created a NEW attractor worse than the one they replaced** — the first cut put *The Simpsons* on **16 of 100 pages** (vs Infinity War's 11), because long-running shows have enormous guest rosters and nearly every working actor has one Simpsons voice or one Grey's Anatomy episode; also "2 Broke Girls via Thomas Kail" for a single episode directed. Fixed with `TV_MIN_EPISODES = 5` on `combined_credits` TV entries (movies unaffected); top repeat fell to The Dark Knight ×5, a flat distribution. Cost of the floors: people rail now empty on 19/100 vs 9/100 — accepted on the trailer-feature precedent that an absent rail beats a misleading one. Device-verified on 4 titles: **Blade Runner 2049** (MLT → Blade Runner 1982 / Upgrade / Chappie; people → Prisoners *via Denis Villeneuve*, La La Land *via Ryan Gosling*, Empire Strikes Back *via Harrison Ford*); **Breaking Bad** — the TV case — (MLT → Better Call Saul / Narcos: Mexico / Snowfall, all series; people → Drive *via Bryan Cranston*, **Westworld** *via Aaron Paul*, Sully *via Anna Gunn*, proving `combined_credits` now returns TV on a TV page); **Oppenheimer** (Interstellar *via Nolan*, Dark Knight *via Cillian Murphy*, Edge of Tomorrow *via Emily Blunt*); **Dune: Part Two** (Prisoners *via Villeneuve*, Interstellar *via Chalamet*, No Way Home *via Zendaya*). TWO BUGS FOUND ONLY ON DEVICE, fixed and re-verified in a second build: (a) two-line titles pushed the year and attribution ~10px out of step with one-line neighbours — `minHeight: 38` was a raw constant while `bodyMd.lineHeight` is `scaleFont(20)`, i.e. device-scaled; now computed as `typography.bodyMd.lineHeight * 2` at render; (b) the score badge's `fontSize`/`fontWeight` were silently dead — `[styles.scoreSource, typography.labelSm]` puts the token last and RN style arrays let the last entry win, so the token overrode the local styles; order swapped. **NOT visually confirmed on device: the loading skeletons and the error/retry branch.** The skeletons became un-raceable precisely because rec 3 cut the fetch from 26 requests to 5 — it resolves faster than `adb input swipe` can reach the foot of the scroll; and an offline attempt to force the error branch failed because OkHttp served the rails from disk cache even after a force-stop (itself a useful finding). Both paths mirror the already-shipped Wikidata loading/error/retry pattern in the same component. Also confirmed the shipped bundle, not just the source: `with_people` and `moreFromCastAndCrew` are absent from `assets/index.android.bundle` and `combined_credits`/`viaPersonName` are present — worth doing, since a 30s "BUILD SUCCESSFUL" with 757 tasks up-to-date looks exactly like a build that skipped the JS bundle. `enrichDiscoverResults` was left alone — Discover still uses it. Shipped — [PR #80](https://github.com/visual-zimbabwe/find-streamer/pull/80) on branch `feat/recommendation-rails`, stacked on `feat/soundtrack-cta` (retarget to `main` once that lands) |
| 2026-07-25 | Results / ResultView | Hero info-pill strip — horizontally scrolling metadata pills (year / runtime / seasons / certification / franchise / language / country) | IMDb title-page metadata line + demoted "Details" section (Apple TV as corroboration on never scrolling metadata) | A tidy metadata row that grew a scroll bar instead of a second line — B for the data, D for the layout, F for anything past the third pill | Approved | Recommendations delivered (6, prioritized), no code written. **Measured, not assumed** — 100 live titles (60 movie / 40 TV, TMDb popular pages 1–3 / 1–2) replayed through the real render path at [ResultView.js:1511-1634](src/components/ResultView.js:1511): real detail endpoints, the app's exact SPARQL ([:247](src/components/ResultView.js:247)), and true `Inter_600SemiBold` glyph advances parsed from the shipped `.ttf` (wrote a minimal TTF table-dir/hmtx/cmap-format-4 reader; note Inter ships a **format 12** cmap first, so the subtable scan must prefer format 4 rather than take the last match). Geometry: A54 384dp screen, `scaleFont(11) → 10dp`, 336dp content width after the 24dp `heroContent` insets. Findings: **86/100 rows overflow**, median row **494dp into a 336dp slot (1.47×)**, worst Evil Dead Burn **657dp = 2.0× screen**; the country pill is **fully visible on 3/100 and entirely off-screen on 58/100**, language fully visible on 19/100; median swipe to reveal country **131dp** (max 289dp). Device-confirmed on the release APK (R5CW31BDEMH) — the model predicted Inception's `United States, United Kingdom` off-screen and the screenshot showed exactly `2010 · 2h 28m · PG-13 · 🈯 French⟨cut⟩`; Barbie identical. `allowFontScaling` is set nowhere in `src/`, so 1.3× pushes overflow to 89/100 and the mean row to 520dp. **The fix is already in the stylesheet and orphaned**: `styles.infoRow` ([:2561-2565](src/components/ResultView.js:2561)) is `flexWrap: 'wrap'` and referenced nowhere in the file. Also found: ~81dp of measured dead vertical space between the row and the SYNOPSIS eyebrow (`heroContent` `bottom: 40` [:2447](src/components/ResultView.js:2447) + `detailsContent` `paddingTop: 32`) — the row scrolls while blank space sits under it; the `wikiLoading` skeletons ([:1584-1589](src/components/ResultView.js:1584)) shimmer in positions that are themselves clipped/off-screen (twin of the phantom skeleton the soundtrack run deleted); pills are bare `View`s with no `accessibilityLabel`, so TalkBack reads an unlabelled sequence; and the row's only emphasis tier (`ratedBadge` [:2580](src/components/ResultView.js:2580), 0.85 opacity + 4× brighter border) is spent on certification and the redundant franchise pill. **A LAYOUT DECISION WAS CHANGED BY MEASURING AT RECOMMENDATION TIME**: wrapping alone (the obvious fix) still yields **two lines on 81/100** titles, whereas demoting language+country makes the hero fit **one line on 100/100** (93/100 at 1.3× font, 85/100 at 1.5×) — so tiering leads and wrapping demotes to a safety net, inverting the order I would have shipped. Recs: (1) move language+country out of the hero into a Details block beside Based On [:1722](src/components/ResultView.js:1722), which shares the same SPARQL call and loading state so nothing new is fetched; (2) swap the `ScrollView` for the orphaned `styles.infoRow`, deleting `infoRowScroll`/`infoFadeOverlay`/the `LinearGradient` [:1627-1633](src/components/ResultView.js:1627), and absorb the second line by reducing `bottom: 40` rather than letting the bottom-anchored `overflow:hidden` stack clip the genre badge upward; (3) move the skeletons with the data; (4) `accessibilityLabel` per pill (NOT `accessibilityRole` — they aren't buttons), mirroring the ratings strip [:1337](src/components/ResultView.js:1337); (5) drop the franchise pill (redundant with the collection rail [:1841](src/components/ResultView.js:1841)) and resolve the emphasis inversion; (6) route the 10 hardcoded `rgba(255,255,255,0.75)` through `colors.onSurface`/`onSurfaceVariant`. **A CRITIQUE POINT WAS DOWNGRADED BY DEVICE EVIDENCE**: I expected white-on-near-white in light mode (`colors.background` `#f7f7f2`, computed contrast **1.05:1**), but forcing `cmd uimode night no` and loading Inception *and* Barbie (deliberately bright poster) showed the screen stays dark — `buildPalette` "always targets dark-mode UI" ([usePosterTheme.js:155-165](src/lib/usePosterTheme.js:155)) and the poster palette overrides the theme at [:465](src/components/ResultView.js:465). Rec 6 was therefore reframed as consistency cleanup with an explicit "skipping it is defensible", the live exposure being only the `palette ?? theme.colors` fallback window; the tell that it's *latent* rather than imaginary is that [usePosterTheme.js:187](src/lib/usePosterTheme.js:187) itself branches `onSurface` to dark ink `#181b21`. Explicitly NOT recommended: raising the 10dp pill text — it resolves from the global `scaleFont(11)` ([responsive.js:31-38](src/utils/responsive.js:31)) whose Android `-1` looks deliberate, so it's a token-level question, not a patch here. Credit due: the pills are **free riders** on a SPARQL call already in flight for Based On/soundtracks/awards (zero incremental requests — the critique is placement, not waste); the data is good (89/100 country, 85/100 language) and `labelOrNull` [:295-302](src/components/ResultView.js:295), hardened during the soundtrack run, leaked **zero bare QIDs** across 100 titles on this path too; and the three always-visible pills (year, runtime/seasons, certification) are exactly the three IMDb keeps in its hero — the ordering instinct was right, the row just outgrew it. Suggested first pass = #1+#2 | **ALL 6 IMPLEMENTED and verified on the release APK on the A54 (R5CW31BDEMH); APK archived as `releases/trova-1.0.10-release-2026-07-25.apk`. 203/203 tests (8 new in `tests/title-meta.test.js`), 0 lint errors; ResultView warnings unchanged at 47 (all 5 `no-unused-vars` and every `react-hooks` warning line predate this change — checked line-by-line, none fall in the edited regions).** User left every open call to me. New pure module `src/lib/titleMeta.js` (`spokenRuntime`, `buildTitleDetailRows`); the hero row is now `styles.infoRow` — the previously orphaned `flexWrap: 'wrap'` style, finally referenced — and `infoRowContainer`/`infoRowScroll`/`infoFadeOverlay`/`skeletonPill`/`ratedBadge` are deleted along with the `LinearGradient` fade. Pill chrome is derived via the existing `colors.x + 'AA'` hex-suffix idiom off `colors.onSurface` (BF/D9 ink, 1F fill, 14/59 borders) rather than hardcoded white. **A MEASUREMENT AT IMPLEMENTATION TIME REVISED THE CRITIQUE'S OWN NUMBER**: the recommendation said the wrap would still fire on ~7/100 titles at 1.3× font, but that model had kept the franchise pill; with the franchise pill *also* dropped (rec #5) the shipped row fits **one line on 100/100 at 1.0×, 1.3× AND 1.5×** — widest real row is Paradise Hotel at 314dp of 336. So the wrap is pure insurance that never fires on measured data, which is worth stating rather than claiming it as a live fix. Proved the guard works anyway by forcing `settings put system font_scale 2.0` on Criminal Minds (tv/4057): the row broke to two lines — `2005 · 19 seasons` / `TV-14` — with nothing clipped and the genre badge + ratings strip still intact, where the old ScrollView would have hidden TV-14 behind the fade. `heroContent` `bottom: 40 → 28` gave it that headroom. Device-verified: **Inception** hero → exactly `2010 · 2h 28m · PG-13`, no clipping/fade (was `… · 🈯 French⟨cut⟩` with country entirely off-screen), and DETAILS renders `LANGUAGES French, English, Japanese` / `COUNTRIES United States, United Kingdom` in full. **LOTR Fellowship (120)** proved section order and both label branches in one shot — BASED ON → DETAILS (`LANGUAGE English` **singular** vs Inception's plural) → Franchise (still renders "The Lord of the Rings Collection · 3 films" despite the hero franchise pill being removed). **Breaking Bad (1396)** TV path → `2008 · 5 seasons · TV-MA`, no runtime pill, pills correctly tinted by that title's green poster palette (the rec-6 derivation working across palettes). **Vampire Princess Miyu (31649)** empty case → Where To Watch goes straight to CAST with no orphan DETAILS header. **NOT visually confirmed on device: (a) the Details loading skeleton** — it sits below the fold at first paint and the shared Wikidata call resolves before `adb input swipe` can reach it (same limitation the rails run hit); **(b) the `accessibilityLabel`s** — `uiautomator dump` still fails with "could not get idle state" because the mesh-orb loop never idles, exactly as the ledger warned, so the labels are code-verified only. Also worth recording: the Details block is placed after Based On, which on a title with a long availability table (Inception: 112 countries collapsed behind "Show all") puts it a fair way down — correct as demotion, but it means Details is not glanceable; if that ever feels too buried, the Production Companies section further down is the natural neighbour to merge with. Bundle-verified rather than trusting a 28s "BUILD SUCCESSFUL": extracted `assets/index.android.bundle` from the APK and confirmed `First aired`/`Countries`/`buildTitleDetailRows` present and all four dead style names absent. Not committed — changes sit uncommitted on `feat/recommendation-rails` |
| 2026-07-26 | Results / ResultView | Awards & Recognition — horizontal award rail (`fetchWikidataAwards` P166/P1411 SPARQL, OMDb regex fallback, per-tile Wikidata deep link) | IMDb title-page awards module (headline rollup line + dedicated `/awards` page grouped ceremony → year → category) | A rail that lists 47 awards for Breaking Bad and never mentions its 16 Emmys — A for the pipeline, F for the arithmetic, F for the layout | Approved | Recommendations delivered (7, prioritized), no code written. **Measured, not assumed** — 100 live titles (60 movie / 40 TV, TMDb popular pages 1–3 / 1–2) replayed through the real `fetchWikidataAwards` + `parseOmdbAwardsFallback`, plus tile widths computed from the shipped `Inter_600SemiBold` glyph advances (same TTF reader approach as the info-pill run; format-4 cmap, 10dp `scaleFont(11)`, `letterSpacing: 0.5`, 384dp A54 screen). Source mix: **22 wikidata / 13 omdb / 65 render nothing**. Defects quantified: (a) **all 22/22 Wikidata-path titles ALSO carry an OMDb awards string that the code discards** ([ResultView.js:667-677](src/components/ResultView.js:667) gates OMDb behind `wikiData.awards.length === 0`) — Breaking Bad's rail shows 32 wins against a real 172 and never says "16 Emmys"; Avatar shows 8 against 91; **Supernatural shows 0 wins for a show with 37**. (b) `formatAwardCounts` prints **"1 Win · 1 Nomination" on 37/159 tiles (23.3%)** because Wikidata stores a won award as both P1411 and P166 and [wikidataAwards.js:189](src/lib/wikidataAwards.js:189) counts each binding. (c) `awardTile` ([:2954](src/components/ResultView.js:2954)) is `minWidth: 120` with **no width/maxWidth — the only rail card in the file without one** (siblings: `scale(120)` [:2722](src/components/ResultView.js:2722)/[TitleRailCard.js:94](src/components/TitleRailCard.js:94), `scale(150)` [:2746](src/components/ResultView.js:2746)); a horizontal ScrollView gives unbounded width in the scroll axis so `numberOfLines={2}` never wraps and the tile stretches to the label → **median tile 305dp on a 384dp screen, 28.3% wider than the whole screen, max 664dp** (Shawshank's ASC award), **median tiles fully visible at rest = 1**, zero fit on 5/22 titles, Breaking Bad's rail = **16,466dp / 43 screen-widths**; `minHeight: 32` [:2974](src/components/ResultView.js:2974) reserves a second line that can never be used. (d) **max wins on any tile = 1, max nominations = 1, across all 159** — so `sortAwardGroups`' two numeric tiers ([:128-134](src/lib/wikidataAwards.js:128)) are inert and the real order is alphabetical: The Simpsons leads with **Kids' Choice Award for Favorite Cartoon** (Peabody is #12, Emmys #3-6), Grey's Anatomy with Genesis Award, CSI with an NAACP Image Award. (e) tap → `wikidata.org/wiki/{QID}`, the entity page for the *award category* listing every winner since 1929, in an external browser. (f) two incompatible information models under one header — Wikidata renders per-category tiles, OMDb renders per-ceremony tiles with real totals, and OMDb's are inert `View`s [:2156](src/components/ResultView.js:2156) indistinguishable from touchable ones. (g) the `wikiLoading` gate [:2083](src/components/ResultView.js:2083) renders eyebrow + 3 skeletons on **every** detail screen, then **65/100 titles unmount the entire section**, jumping content upward (same phantom-skeleton class the soundtrack run deleted); 23 of those 65 have real OMDb strings (`7 wins & 11 nominations total`) that render nothing because `OMDB_AWARD_DEFS` [:297-326](src/lib/wikidataAwards.js:297) only knows Oscars/Emmys/Globes, and even on the 13 it parses the totals clause is always discarded. (h) a11y label [:2148](src/components/ResultView.js:2148) is `Open ${label} on Wikidata` — `TouchableOpacity` collapses children so the count is never announced. **A MEASUREMENT AT RECOMMENDATION TIME REORDERED THE FIX**: I first modelled a fixed-width tile + ceremony-prefix strip, but even at 150dp with the prefix stripped only **64%** of labels fit two lines (raw: 35%) — and stripping "Primetime Emmy Award for" is unsafe anyway because **53.5% of tiles have no logo** to carry the ceremony. Modelling a **ceremony-level regroup** instead produced counts that are *exactly right*: Avatar → Academy Award **3W/9N** (won 3 Oscars of 9 noms ✓), Interstellar → **1W/5N** ✓, Shawshank → **0W/7N** ✓, GoT → Primetime Emmy 5W ✓. **The P166+P1411 "double count" is not a bug at the ceremony level — it produces the correct industry-convention total.** So the data was always right and the *grouping level* was wrong; the fix is a regroup, not a subtraction, and it retires four defects at once. Grouping cuts tiles/title from median 3 / mean 7.2 / max 47 → median 3 ceremonies, 19/22 titles at ≤5 (Breaking Bad 47→23, Avatar 15→5). Recs: (1) `parseOmdbAwardTotals` beside [`parseOmdbAwardsFallback`](src/lib/wikidataAwards.js:337) → headline line under the eyebrow, OMDb no longer gated behind empty Wikidata (regex must handle `Won 3 Oscars. 91 wins & 131 nominations total`, `Nominated for 7 Oscars…`, bare `21 wins & 42 nominations total`, `2 wins total`, singular `1 win & 1 nomination` with no "total" — covers all 58 titles with award data vs 35 today); (2) regroup [`parseAwardQueryResults`](src/lib/wikidataAwards.js:222) on ceremony via `/^(.*?\bAwards?\b)(?:\s*\([^)]*\))?\s+(?:for\|of)\s+/i`, categories demoted to a detail sheet; (3) **merge the sources rather than gating** — OMDb counts for Oscars/Emmys/Globes, Wikidata for the rest, because Wikidata undercounts long-running TV (Breaking Bad Emmy rollup 6 vs OMDb's 16) and without this rec 1's headline sits directly above a contradicting tile; (4) `width: scale(140)`, `minHeight` computed as `typography.labelSm.lineHeight * 2` (the device-scaling bug the rails run already hit), `rank` field on the existing [`CEREMONY_PATTERNS`](src/lib/wikidataAwards.js:6) for prestige sort, cap ~6 + See all (fires on only 3/22) — flagged that institutional names (ASC, Critics' Choice Television, National Board of Review) will still clip at 2 lines, decide 2 vs 3 against real output; (5) gate the section on the synchronously-known OMDb string to kill the phantom skeleton; (6) tap → `imdb.com/title/{imdbId}/awards` (imdbId present on 98/100), every tile tappable, following the shipped [ratingBadges.js](src/lib/ratingBadges.js) deep-link pattern; (7) fold the count into the a11y label, mirroring [:1337](src/components/ResultView.js:1337). **Explicitly NOT recommended**: P585 award years — requires moving from `wdt:` truthy statements to `p:`/`ps:`/`pq:` statement nodes on a query that's already two round trips, and after #1/#2 the year is detail-sheet material. Credit due: the fetch architecture is right — awards ride the `Promise.all` at [:270-279](src/components/ResultView.js:270), post-paint, **median 281ms / p90 598ms** for two SPARQL queries plus Commons resolution, degrading to OMDb on error with no dead section; `AwardLogoImage` [:128-153](src/components/ResultView.js:128) is exemplary (`onError` → trophy fallback with a `uri`-keyed reset so a recycled component can't inherit the last title's failure — the discipline the trailer feature had to be retrofitted with); `inheritCeremonyLogos` [:192](src/lib/wikidataAwards.js:192) is a genuinely good idea and is what stops 53.5% of tiles being trophy glyphs; **cleanest Wikidata label path in the app at 2 bare QIDs in 159 tiles (1.3%)** vs the soundtrack run's 17.5%; Commons resolution shared not duplicated [:1](src/lib/wikidataAwards.js:1); and no mainstream streaming-finder shows awards at all. Suggested first pass = #1+#2+#3 (one coherent change to `wikidataAwards.js` plus a headline line in the component). **NOT device-verified at recommendation time — no device was attached** (`adb devices` empty); all numbers are from live API replay + code + glyph measurement, and the tile-width figures rest on the unbounded-width-in-a-horizontal-ScrollView layout claim, which was code-reasoned rather than screenshot-confirmed (device testing later confirmed it). **ALL 7 IMPLEMENTED and verified on the release APK on the A54 (R5CW31BDEMH); APK archived as `releases/trova-1.0.11-release-2026-07-26.apk`. 217/217 tests (14 new in `tests/wikidata-awards.test.js`), 0 lint errors, ResultView warnings unchanged at 47.** New `src/components/AwardsSheet.js`; `wikidataAwards.js` gained `parseOmdbAwardTotals`, `formatAwardTotals`, `ceremonyNameFromLabel`, `categoryNameFromLabel`, `ceremonyKeyFromName`, `ceremonyRank`, `groupAwardsByCeremony`, `mergeOmdbCeremonyCounts`, `buildAwardCeremonies`, `awardCountLines`, `spokenAwardCounts`. Grouping runs at render (not fetch) so watchlist entries cached by an older build in the flat per-category shape group correctly too — the same stale-cache trap the soundtrack run hit. **Re-measured the SAME 100-title sample through the shipped module: titles rendering something 35 → 58; phantom skeleton (header+skeletons then full unmount) 65 → 0; tiles/title median 3 / mean 7.2 / max 47 → 2 / 2.6 / 12; tiles printing "1 Win · 1 Nomination" 37/159 → 0; median tile width 305dp → 122dp; widest tile 664dp → 122dp; tiles wider than the whole screen 28.3% → 0.** **FOUR THINGS THE RECOMMENDATIONS GOT WRONG OR MISSED, all caught by measuring and by the device, not by reasoning:** (1) *`scale()` bases on 390, not 375* — the first cut at `scale(140)` gave a 102dp text box and "3 Wins · 9 Nominations" (123dp) overflowed; fixed by splitting counts onto two lines (`awardCountLines`), which is better design anyway since the win count deserves the emphasis. (2) **The rail lives inside `detailsContent`'s 24dp page padding, so its viewport is 336dp — exactly two 150dp tiles wide with ZERO peek**; on device the rail looked like it had precisely 2 items and the "See all" tile was unreachable. Fixed with `awardsRail { marginRight: -24 }` bleeding the rail to the screen edge plus a narrower `scale(124)` tile → 2 tiles fully visible with 92dp of the third showing. The recommendation's tile-width fix was correct in isolation and wrong in context. (3) **The ceremony tile was rendering a photo of a laureate as the award's emblem** — Breaking Bad's Emmy tile resolved to a headshot of Julia Garner, because P18 on an award entity is a photograph and the old `awardLogoFromBinding` returned a bare URL with no provenance. Added a `logoSource` (icon/logo/image) rank; ceremony tiles now accept emblems only and fall back to the trophy glyph. Cost, stated honestly: tiles with no logo 53.5% → **65.9%** — a deliberate trade of coverage for never showing a stranger's face as an award. Also found that **four of the five `fallbackFile` Commons names shipped in `CEREMONY_PATTERNS` do not exist** ("Golden Globe trophy.png", "Emmy Statuette.jpg", "BAFTA mask.svg", "Hugo Award rocket.svg") — that fallback had been dead for everything but the Oscar; replaced with verified files and added SAG/DGA. Commons hosts no free Emmy statuette, so the Emmy tile falls through to the Television Academy logo OMDb already supplies. (4) **THE BIGGEST FIND, and it was pre-existing, not mine: the item-lookup used the wrong Wikidata properties.** `P4985` is **"TMDB person ID"**, not TV series — so every TV title unioned in whichever *person* shared its numeric id, and Breaking Bad (TMDb 1396) was merging in the awards of **Hanna Krall (Q508420), a Polish writer**, rendering "Samuel-Bogumil-Linde prize", "The Julian Tuwim Literary Award" and "Warsaw Literary Prize" as ceremony tiles on a New Mexico meth drama. `P9721` is **"image of entrance"** — not a TMDb id at all — so the movie branch matched nothing and movies silently depended on the IMDb id alone. Corrected to **P4983 (TMDB TV series ID)** and **P4947 (TMDB movie ID)** in BOTH `wikidataAwards.js` and `ResultView.js`'s main enrichment query (the same two wrong ids were in both, so languages/countries/basedOn/soundtracks were contaminated the same way). Fixing it immediately inflated every count 4× because both UNION clauses then matched the same item and the parser counts bindings — fixed by adding `SELECT DISTINCT` to both award queries; with `wdt:` truthy paths an award appears once per item so DISTINCT is safe. Net effect on Breaking Bad: 23 ceremonies → 12. Device-verified on 6 titles: **Breaking Bad** (headline "Won 16 Primetime Emmys · 172 wins & 269 nominations" — the fact a 47-tile rail never stated; 6 tiles + "See all +6 more"; sheet lists 16 Emmy categories with Won/Nominated and winners sorted first; no Polish prizes); **Avatar** (headline "Won 3 Oscars · 91 wins & 131 nominations"; Academy Award tile = gold Oscar emblem, "3 Wins / 9 Nominations" — the exact counts that were previously spread across 9 tiles each claiming "1 Win · 1 Nomination"); **House of the Dragon** (the OMDb-only Emmy tile now named "Primetime Emmy Award" and ranked FIRST, ahead of the Wikidata Golden Globe — it used to read "Emmys" and sort last because `\bemmy\b` never matched the plural); **Moana** (no award data anywhere → Crew goes straight to Production Companies, no orphan header, no phantom skeleton); **The Odyssey** (OMDb "2 wins total" with no recognisable ceremony → headline-only section reading "2 wins", where the old build showed nothing at all — this is the 35→58 coverage gain on screen); and the **IMDb handoff** (tapping "View full awards on IMDb" opened Chrome on `imdb.com/title/tt1119.../awards` — House of the Dragon's own awards page, versus the old destination, a wikidata.org page about an award category listing every winner since 1929). **NOT verified on device: the loading skeleton** (the shared Wikidata call resolves before `adb input swipe` can reach the section, the same limitation the rails and info-pill runs hit) **and the `accessibilityLabel`s** (`uiautomator dump` still fails with "could not get idle state" because the mesh-orb loop never idles — code-verified only). Gotchas worth recording: a `sed` with an unanchored `result?.imdbId` pattern also rewrote that identifier inside the Wikidata effect's dependency array, putting a `const` declared 100 lines later into a temporal dead zone — caught because eslint's react-hooks compiler bailed and the warning count collapsed from 48 to 7, which is the tell that the file failed to compile rather than got cleaner. And the bundle must be checked, not the build log: two `assembleRelease` runs reported "BUILD SUCCESSFUL in 27s / 757 up-to-date", so each APK was confirmed by extracting `assets/index.android.bundle` and grepping for the new strings and the absence of `P4985`/`P9721`. Not committed — changes sit uncommitted on `feat/recommendation-rails` |
| 2026-07-24 | Results / ResultView | Hero ratings strip — 4-source critic badges (TMDb/IMDb/RT/Metacritic), each tappable to its source | Google movie knowledge-panel ratings cluster (RT icon-flip + Metacritic score box as secondary mirrors) | Four brand-perfect badges, two that lie about the score and two that link to a search box — a C-tier aggregator wearing A-tier logos | Approved | All 5 implemented and verified on release APK on the A54 (R5CW31BDEMH), branch `fix/ratings-strip-fidelity`. New `src/lib/ratingBadges.js` holds the slug/threshold logic (unit-tested, `tests/rating-badges.test.js`, 6 tests; suite 160/160, lint 0 errors). (1) RT + Metacritic now deep-link via title slug — RT underscores `/m/{slug}`, Metacritic hyphens `/movie/{slug}`, TV variants, search only when the title yields no slug; tapping RT on The Emoji Movie opened `rottentomatoes.com/m/the_emoji_movie` directly (was a search page). (2) tomato flips on the 60% line — verified red 🍅 at 93% (Killers of the Flower Moon) vs 🤢 splat at 6% (Emoji Movie). (3) Metacritic tile colored by threshold — verified green M at 89 (KOTFM) vs red M at 12 (Emoji Movie); yellow 40–60 takes dark text. (4) units restored via a `ScoreValue` sub-component with a dimmed suffix — `7.4/10`, `3.5/10`, `6%`, `12/100` all render. (5) a11y labels now carry the score + Fresh/Rotten ("…Rotten Tomatoes 6 percent, Rotten. Opens Rotten Tomatoes.") — code-verified (no uiautomator dump; mesh-orb loop blocks idle). Metacritic badge still sits under the floating share/bookmark buttons on long titles (pre-existing hero overlap, out of scope). Not committed — changes sit on the branch |
| 2026-07-26 | Results / ResultView | Production Companies rail — logo-only tiles (`logo_path` filter, platform-forked "halo", `onCompanyPress` → studio catalog screen) | IMDb title-page "Production companies" line + its company page (Disney+/Apple TV brand-tile rows as corroboration on when a *logo* rail is the right form) | A rail of invisible black rectangles that opens a studio page claiming Columbia Pictures made 22 films — A for the wiring, F for the eyes | Approved | Recommendations delivered (8, prioritized), no code written. **Measured, not assumed** — 100 live titles (60 movie / 40 TV, TMDb popular pages 1–3 / 1–2) replayed through the real render path, 246 rendered tiles, 188 unique logos decoded pixel-by-pixel with a PNG luminance reader, plus device verification on the release APK on the A54 (R5CW31BDEMH). Defects quantified: (a) **68.3% of on-screen tiles (168/246) are dark ink on a dark surface**, 51.2% near-black, and **90 of 188 unique logos measure exactly 0.0 luminance** — pure `#000000` artwork (Walt Disney Pictures, Pixar, Lucasfilm, Columbia, Illumination, Miramax, Blumhouse, Syncopy); **measured contrast off a device screenshot of Blade Runner 2049's Columbia wordmark = 1.27:1** against a luminance-31 surface (WCAG non-text floor 3:1). (b) **The Android halo cannot draw**: `shadowColor`/`shadowOffset`/`shadowOpacity`/`shadowRadius` map to `setOutlineAmbientShadowColor`/`setOutlineSpotShadowColor` ([BaseViewManager.java:275-285](node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/uimanager/BaseViewManager.java:275)), which shadow the view's *outline* — a rectangle of the 80×40 box, not the glyph — and the halo `View` has no background, so nothing casts; best case a black rectangle behind black ink. Meanwhile the iOS branch (`shadowColor: '#ffffff'`, which *would* work since iOS derives the shadow from the image alpha) is **dead code — [app.json:9](app.json:9) is `"platforms": ["android"]`**. The right fix was built for the platform the app doesn't ship on, a no-op for the one it does. (c) **The tile is two `<Image>` elements and nothing else** ([ResultView.js:2321-2348](src/components/ResultView.js:2321)) — no name, no text; **151 of 188 companies appeared exactly once** across the sample, so the unrecognizable long tail is the majority (Torridon Films, 16:14 Entertainment, Pascal Pictures, Domain Entertainment). (d) `.filter((company) => company.logo_path)` ([tmdb.js:561](src/lib/tmdb.js:561)) **silently deletes 80 of 326 credits (24.5%)** — Game of Thrones hides 4 of 6; 6 titles have companies and render no section at all. (e) **The destination screen is the rails bug unfixed in the sibling function** — [`fetchProductionCompanyCatalog`](src/lib/tmdb.js:1754) still uses `sort_by: 'vote_average.desc'` + `vote_count.gte: 5` ([:1757-1761](src/lib/tmdb.js:1757)), the exact shape the rails run replaced with a 200-vote floor: across 10 companies **37.7% of cards under 200 votes, 28.3% under 50**; Walt Disney Pictures opens with *Radio Disney Music Awards* (2013) **9.6 from 7 votes** then a 1989 safety PSA; Universal Pictures opens with *Brighter Days Ahead* **9.5 from 8 votes**, **median vote count 14**. (f) both `/discover` calls request `page: 1` only and `countText` ([FilmographyScreen.js:73](src/components/FilmographyScreen.js:73)) prints that slice as a total — device-verified **"22 TITLES" for Columbia Pictures, where TMDb returns 1,544** for the app's own query; 8/10 sampled companies undercount. (g) the studio logo is cropped into a **64dp circle** — [FilmographyScreen.js:98-106](src/components/FilmographyScreen.js:98) passes no `resizeMode` so `MediaArtwork` defaults to `cover` ([MediaArtwork.js:11](src/components/MediaArtwork.js:11)); on device Columbia's header avatar reads **"MBI / URE"**. (h) the empty state says "credits matching this person" ([:150](src/components/FilmographyScreen.js:150)) on a screen that knows `role === 'company'`. (i) tile pitch 132dp into a 336dp viewport = **2 fit at rest, 41/100 titles overflow**, no peek (the same viewport trap the awards run hit, without the `marginRight: -24` bleed that fixed it). **A MEASUREMENT KILLED THE OBVIOUS FIX**: TMDb *does* serve alternate logo assets (Columbia 11, Disney 9), so a `pickCompanyLogo` mirroring the shipped `pickTitleLogo`/`pickHeroBackdrop` looked like the third instance of a pattern that worked twice — I measured every alternate for the 30 darkest logos and **only 7/30 (23%) have a lighter variant**; for Pixar, Lucasfilm, Blumhouse, Miramax, Syncopy, 21 Laps and Seven Bucks every asset TMDb holds is black. The asset pool does not contain the fix, so the repair must be render-side. Recs: (1) **name on every tile** + delete the `logo_path` filter (root fix — the filter only exists because the tile has nowhere to put a name; retires (c) and (d) together); `width: scale(124)` per `awardTile` (**`scale()` bases on 390 not 375** — awards-run gotcha), `minHeight` computed as `typography.labelSm.lineHeight * 2` never a raw constant (rails-run gotcha), `numberOfLines={2}` with long institutional names decided against real output; (2) delete the whole halo fork ([:3152-3176](src/components/ResultView.js:3152)) and put the logo on a near-white rounded plate — deterministic, covers 100% vs rec-3's 23%, **explicitly NOT the hero-artwork scrim that was built and reverted** (that failed as a `LinearGradient` on `absoluteFill` inside the inset `heroContent`, drawing a hard-edged dark rectangle over photographic art; this is a bounded opaque tile plate where the shape IS the intent); (3) **do NOT build `pickCompanyLogo`** — recorded as a recommendation because it is the obvious and wrong next move; (4) `popularity.desc` not `vote_average.desc` + reuse `MIN_RAIL_VOTES`/`ABSOLUTE_MIN_RAIL_VOTES` from [railPicker.js](src/lib/railPicker.js) rather than inventing a second threshold; `mapRow` ([:1775-1790](src/lib/tmdb.js:1775)) doesn't capture `vote_count` — the same three-line gap the rails fix had to close first; scope to the company path, leave `fetchPersonFilmography` alone; (5) return the discarded `total_results` ([:1792-1796](src/lib/tmdb.js:1792)) and render an honest count (or none) on the company branch — a person's filmography really is complete, so this is company-only; (6) `resizeMode="contain"` + a wider rounded-rect plate for `role === 'company'` (a 64dp circle cannot hold a wordmark at any fit mode — `cover` crops, `contain` shrinks past reading size); (7) `marginRight: -24` bleed pairing with the `paddingRight: 24` already on `productionScroll` ([:3141](src/components/ResultView.js:3141)); (8) role-aware empty-state copy + set `accessibilityLabel` unconditionally ([:2316-2318](src/components/ResultView.js:2316) leaves an `accessibilityRole="button"` with no name when `onCompanyPress` is absent). **Explicitly NOT recommended and unmeasured**: rolling subsidiaries up to a parent company (Columbia → Sony) — `with_companies` doesn't expand the hierarchy and merging them is a product decision, not a defect. **A TRAP FROM TWO PRIOR RUNS DOES NOT APPLY HERE, and it was checked rather than assumed**: `productionCompanies` is written only at [tmdb.js:560](src/lib/tmdb.js:560) and read only at [ResultView.js:2296](src/components/ResultView.js:2296), and no storage module references it — so unlike the soundtrack and awards runs there is no cached-watchlist branch, and a parse-time fix needs no presentation-time twin. Credit due: **no phantom skeleton** — the section rides `result`, present at first paint, and renders nothing when empty, which is the defect class the soundtrack and awards runs both had to delete, correct here by construction; `onCompanyPress` wired identically across all four nav stacks ([HomeStack.js:88](src/navigation/HomeStack.js:88), Discover, Search, Watchlist) with a `disabled={!onCompanyPress}` guard and matching `accessibilityState`; `role: 'company'` reuses `FilmographyScreen` instead of forking a new one and `roleLabel` correctly renders "TITLES FROM"; `Haptics.selectionAsync()` matches every other rail in the file; and browsing by studio is a genuinely good idea no mainstream streaming-finder offers. Suggested first pass = #1+#2, then #4+#5 as a pair. Device evidence captured at critique time: Blade Runner 2049's rail (Alcon / Columbia / a sliver of Scott Free, no glow of any kind, 5 tiles of which 1 hidden by the filter) and the Columbia studio screen ("MBI/URE" avatar, "22 TITLES", #1 *Spider-Man: Brand New Day* 9.0 from 14 votes) | **ALL 8 IMPLEMENTED and verified on the release APK on the A54 (R5CW31BDEMH); APK archived as `releases/trova-1.0.12-release-2026-07-26.apk`. 223/223 tests (6 new in `tests/rail-picker.test.js`), 0 lint errors; ResultView warnings 47 → 48, the one addition being a `react-hooks/set-state-in-effect` on the new `CompanyLogoImage`'s `uri`-keyed reset — the identical line `AwardLogoImage` already trips (HEAD baseline confirmed by linting `git show HEAD:` output in place, not a copy: linting the file outside the project tree reported 1 warning instead of 47, the same false-clean tell the awards run recorded).** The tile is now a light `#f2f2ee` plate above an always-rendered company name; the `logo_path` filter is gone; `rankCompanyCatalog` is a new export in `railPicker.js` (not a private helper in `tmdb.js`) so the studio grid and the rails share one set of floors; `fetchProductionCompanyCatalog` sorts `popularity.desc`, captures `voteCount`/`popularity`, and returns `total_results`, which `usePeopleController` threads to `FilmographyScreen` through all four nav stacks. **A RE-MEASURE CAUGHT A REGRESSION I HAD INTRODUCED, before the device ever saw it**: the first cut of rec 2 fixed the 68.3% dark-ink majority and broke the light-ink minority — 23/188 logos (Atomic Monster, Wolf Entertainment, Starburns) are near-white marks that fell to 1.06:1 against the light plate. So I swept plate luminance across the real 246-tile distribution instead of picking a tone by eye. The result kept the light plate but for a reason I had not had before: **the distribution is bimodal** — marks are near-black or near-white with little between — so every mid-grey compromise loses to both tiers at once. Share of tiles reaching 3:1: dark surface (before) **25.2%**, mid-grey #8a8a84 **63.0%**, #f2f2ee **88.6%**. That sweep is recorded as a comment on `productionLogoPlate` specifically so the next person doesn't 'fix' the residual with a grey and make it worse. The residual ~11% is real but milder on screen than the number suggests, and device-checked rather than assumed: Wolf Entertainment's blue wordmark reads perfectly on the plate and only its white wolf-head graphic washes out — the company is still identified, which is exactly the fallback argument rec 1 rests on. **Re-measured the SAME 100-title sample through the shipped code**: tiles rendered **246 → 326** (the 24.5% the filter deleted, restored); titles rendering the section **92 → 98**; black-ink contrast on the plate **1.27:1 → 18.71:1**; cards under 200 votes on a studio page **37.7% → 0.0%**; under 50 votes **28.3% → 0.0%**; median vote count of an on-screen card **~47 → 3,137**. Device-verified on 5 titles: **Blade Runner 2049** (Alcon / Columbia / Scott Free legible and named, third tile now peeking thanks to the `marginRight: -24` bleed; scrolling the rail reaches **Bud Yorkin Productions**, the logo-less company the filter used to delete, on the muted plate); **The Lion King** (pure-black Disney marks — measured 0.0 luminance — crisp on the plate, and the plate holds against that title's maroon poster palette, not just the default dark grey); **Columbia Pictures studio page** (header **"TOP 20 OF 670"** where it read "22 TITLES"; full wordmark avatar where it read **"MBI / URE"**; #1 Spider-Man: No Way Home at 22,303 votes where it was *Spider-Man: Brand New Day*, 9.0 from 14); **Walt Disney Pictures studio page** — the headline case — (**"TOP 21 OF 254"**, led by The Lion King and Pirates of the Caribbean, where it opened with *Radio Disney Music Awards* 9.6 from 7 votes then a 1989 safety PSA); **Law & Order: SVU** (the TV path, and all three tile states in one frame: light-ink partial on Wolf, dark-ink perfect on Universal Television, no-logo muted plate on Studios USA); and **Borderline** (movie/1727780), which was one of the 6 sampled titles that rendered **no section at all** — it now shows Gen1 Entertainment, LLC, and tapping through reaches the empty state reading **"We couldn't find any titles from this studio."** (was "credits matching this person"). **NOT verified on device: the `accessibilityLabel`s** — `uiautomator dump` still fails with "could not get idle state" because the mesh-orb loop never idles, exactly as the ledger has warned on three prior runs; code-verified only. Bundle-checked rather than trusting the build log, which again read "BUILD SUCCESSFUL in 29s / 757 up-to-date": extracted `assets/index.android.bundle` and confirmed `rankCompanyCatalog`, `popularity.desc`, `f2f2ee`, `business-outline` and the new empty-state string present, and `productionLogoHalo` absent. One honest note on the count: it reads "Top 20 of 670" for Columbia, not 1,544, because the query now carries a 50-vote floor — 670 is the size of the catalogue actually being paged, so the number and the grid agree, which was the point. **A THIRD INSTANCE OF THE SAME DEFECT IS STILL LIVE and is deliberately out of scope**: the critique noted `getSimilar` and `fetchProductionCompanyCatalog` shared a `vote_average.desc` + tiny-floor shape; grepping after the fix, [tmdb.js:819](src/lib/tmdb.js:819) and [tmdb.js:1352](src/lib/tmdb.js:1352) plus [homeFeed.js:162/:216](src/lib/homeFeed.js:162) still sort that way. Discover's is a user-chosen "Highest Rated" option and legitimate; the other three were not examined and are worth their own look. Not committed — changes sit uncommitted on `feat/awards-recognition`. |
| 2026-07-26 | Results / ResultView | "Based On" source-material section — Wikidata `P144` SPARQL, three render branches (skeleton / retry / cards), tap → raw `wikidata.org/wiki/Q…` | Google movie knowledge-panel "Based on" entity chip → book knowledge panel (Wikipedia Page Previews as corroboration for the destination point) | A− fact, D+ card, F destination — the app knows Blade Runner came from a Philip K. Dick novel and spends it on a link to a database admin page | Approved | Recommendations delivered (8, prioritized). Core finding: the section announces itself before it knows it has anything to say — `wikiLoading` unconditionally draws eyebrow+skeleton ([ResultView.js:1793](src/components/ResultView.js:1793)) at scroll position 3, then unmounts for every original screenplay, and the error branch ([:1810](src/components/ResultView.js:1810)) tells those same users "Couldn't load source material" about content that never existed. This is the SAME bug already fixed for Awards ([:738-748](src/components/ResultView.js:738)) and left un-generalized. Hidden dependency found: the Based On error row is the ONLY retry for the whole SPARQL call (languages, countries, soundtracks, Wikidata awards) per the comment at [:1894-1898](src/components/ResultView.js:1894) — so #1 and #2 must ship together or hiding the section deletes error recovery. Recs: (1) gate on a TMDb `keywords` pre-signal — one token added to `append_to_response` at [tmdb.js:440](src/lib/tmdb.js:440), zero extra requests, mirrors the OMDb-string gate used for Awards; (2) rehome the retry into Details with honest copy; (3) peek sheet (existing cast-rail idiom) instead of `Linking.openURL` to a raw QID at [:708-712](src/components/ResultView.js:708); (4) cover art via `P18` in the same query — `resolveSoundtrackCovers` [:385](src/components/ResultView.js:385) already does this for albums, books are the one text-only entity on the screen; (5) `getSpecificType` [:407-411](src/components/ResultView.js:407) is non-deterministic (first non-generic type from a binding-order Set) — needs a ranked preference list; (6) split the one clamped `numberOfLines={2}` line [:1837-1859](src/components/ResultView.js:1837) into title + byline slots; (7) widen beyond `P144`/`P50` — add `P941` inspired-by and `P110` illustrator so manga/comic sources stop being dropped nameless at [:361](src/components/ResultView.js:361); (8) SEPARATE SCOPE — reverse `P144` lookup filtered to TMDb-ID-bearing entities for "other adaptations of this source", opening Trova's own detail screens; the one move no competitor can make because Trova already knows global availability. **All 8 implemented and verified on release APK on the A54 (R5CW31BDEMH); 250/250 tests, 0 lint errors.** #1 the pre-signal is TWO signals, not one: TMDb keywords alone were right 11/15 (Dune and Shogun carry no `based on` keyword at all), so `getCredits` now also reports `hasSourceCredit` from writing-department job titles (Novel/Graphic Novel/Book) — 18/20 alone — and `resolveMatch` ORs them into `result.isAdaptation`. Two keywords are excluded as measured false positives: `based on true story` (Chernobyl) and `based on short film` (Whiplash) name no discrete work, so Wikidata has no P144 and the section would reserve space it must give back. End-to-end gate probe over 18 titles: 16 ideal, **0 phantom skeletons**, 2 late appearances (Stranger Things, Chernobyl) — the failure direction is right, a section arriving late beats one that vanishes. #2 retry rehomed to Details with copy naming the request (`Couldn't load extra details`); verified on device by cutting the radio — Based On correctly renders NOTHING while Details shows the retry, and tapping it restored Based On AND Details together, which is the proof the retry belongs at request level. #3 `BasedOnSheet.js` peek sheet (eyebrow/title/cover/byline + Other adaptations + `View on Wikidata` demoted to a footer link). #4 P18 covers via the same SPARQL + `resolveBasedOnCovers`; **covers rendered as empty grey boxes until switched from RN `Image` to `expo-image`** — upload.wikimedia.org answers 403 to Android's OkHttp User-Agent (same trap `AwardLogoImage` hit); new exported `SourceCoverImage` carries the uri-keyed failure reset. #5 `pickSourceType` ranked preference list replaces the first-non-generic scan, and a purely generic type set now renders NO label — Wikidata returns P31=`literary work` alone for The Shining, Shogun and Blade Runner's novel, and a gold `Literary work:` badge is database vocabulary. Creator names are sorted too (Watchmen alternated `Dave Gibbons and Alan Moore` between fetches). #6 title and byline split into separate clamps. #7 P941 inspired-by (tagged and sorted after P144), P110 illustrator, and one hop past a screenplay to its own source. #8 `fetchOtherAdaptations` reverse-P144 query filtered to TMDb-id-bearing entities, fetched lazily on sheet open, hydrated through `getTitleMatchById` and opened via `onSelectSimilar` — verified by tapping Watchmen's source through to Watchmen: Chapter I. Two things the device forced: Stranger Things carries NINE P941 statements, so a `BASED_ON_CARD_CAP` of 3 plus a `+N more` line was added; and the per-card `Inspired by` prefix is suppressed when the section eyebrow already says INSPIRED BY. Sheet size went medium→large after `medium` cut Watchmen's list to 2 of 4 rows. Known nit, left alone deliberately: Watchmen's source labels as `Limited series` (Wikidata's own comics term) which could read as the 2019 TV series — mapping it to `Comic series` would be a fabricated inference that mislabels a genuine TV source. New `src/lib/basedOn.js` + `src/components/BasedOnSheet.js` + `tests/based-on.test.js` (26 tests). Not committed — changes sit uncommitted on branch `feat/production-companies`; APK archived at `releases/trova-1.0.13-release-2026-07-26.apk` |
| 2026-07-27 | Results / ResultView | Detail-screen scroll chrome — parallax hero, collapsing sticky title bar, floating back/share/bookmark pills | Spotify album/artist page collapsing header (Google Play store listing as Material corroboration on scrim-before-title) | A native-driven header with a JS brain that never wakes up — A for the motion, C for the type, F for the one line of logic it was asked to run | Approved | Recommendations delivered (7, prioritized), no code written. **Device-proven on the release APK on the A54 (R5CW31BDEMH), plus 100 live TMDb titles measured through the shipped `Manrope_700Bold`/`Inter_600SemiBold` glyph advances (same TTF reader approach as the info-pill and awards runs; format-4 cmap, 384dp screen).** Findings: (1) **the bottom-nav auto-hide wired into this screen never fires** — the `listener` passed to a native-driven `Animated.event` ([ResultView.js:439-454](src/components/ResultView.js:439)) is not invoked on RN 0.83.6 + Fabric (`newArchEnabled=true`); reproduced by scrolling Home until the nav hid, tapping a poster, and landing on Breaking Bad at scroll offset 0 with the nav STILL hidden, though `applyBottomNavScrollVisibility` calls `setVisible(true)` at any offset <= 40 ([BottomNavVisibilityContext.js:46](src/context/BottomNavVisibilityContext.js:46)); two mid-drag captures on the detail screen never hide it, while the identical drag on Home does. The fossil: `useBottomNavScroll` is **imported at [:19](src/components/ResultView.js:19) and never used** — the working pattern every other screen uses, traded away for the native driver. `Animated.event` appears exactly once in `src/`, so the blast radius is this screen only. (2) **the bar fades in translucent** — `opacity` sits on the outer `Animated.View` [:1437](src/components/ResultView.js:1437) and the Android backdrop is a CHILD [:1447](src/components/ResultView.js:1447), so it inherits the parent alpha; screenshot at ~scrollY 400dp (sticky opacity 0.70) shows the SVU logo art and the SYNOPSIS eyebrow reading straight through the bar under a greyed "Law & Order: Spec…" — a ~100dp window with two titles on screen at once. (3) **37/100 titles truncate at ~17 characters**: `stickyTitleContent` [:2677-2685](src/components/ResultView.js:2677) pads 72 left / 120 right = **192dp of a 384dp screen**, leaving a 156dp title box after the year, while the buttons actually occupy 56dp and 104dp; the model predicted `Law & Order: Speci…` and the device rendered `Law & Order: Spec…` (off by one, the reserved ellipsis). Variants measured: real-footprint padding 37→30, + drop the year 30→**19**, + a 14px tier 19→**11**, two lines →0. The hero already ships a deterministic title shrink-tier system; the bar, at a fifth the width, got none of it. (4) **choreography is half device-scaled, half hardcoded** — the bar keys off `HERO_HEIGHT = verticalScale(480)` [:133](src/components/ResultView.js:133) while the hero fade uses literal `260`/`430` [:1331](src/components/ResultView.js:1331); measured `HERO_HEIGHT` on this device = **430dp** (window height ~756dp), so they align by pure coincidence here and drift ~80dp on a 900dp window. (5) **the opacity-0 bar stays in the a11y tree** — `pointerEvents` in RN Android only gates touch targeting ([ReactViewGroup.kt:278-308](node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/views/view/ReactViewGroup.kt:278), `canBeTouchTarget`) and never sets `importantForAccessibility`; Android `isVisibleToUser()` ignores alpha. **NOT device-verified — `uiautomator dump` still fails with "could not get idle state", for the reason in (7).** (6) three dead iOS branches on an android-only app (`app.json:9`): `BlurView` [:1449](src/components/ResultView.js:1449), `contentInsetAdjustmentBehavior="never"` [:1468](src/components/ResultView.js:1468), and the `-HERO_HEIGHT` pull-to-zoom leg [:1313-1322](src/components/ResultView.js:1313) (Android `getScrollY()` clamps at 0; the Android 12+ stretch is an `EdgeEffect` draw, not a negative offset) — plus `fontWeight: '800'` in `styles.stickyTitle` [:2687](src/components/ResultView.js:2687) silently dead because `typography.titleMd` is spread AFTER it at [:1453](src/components/ResultView.js:1453), the same last-style-wins bug the rails run fixed on the score badge. (7) **the 18s mesh-orb `Animated.loop` [:973-993](src/components/ResultView.js:973) never stops while mounted** — native-driven and cheap, but it keeps the app permanently non-idle, which is why `uiautomator dump` has failed and every a11y label shipped by the soundtrack, awards, info-pill, rails and production-companies runs is code-verified only. Recs: (1) move the nav logic to `onScrollBeginDrag`/`onScrollEndDrag`/`onMomentumScrollEnd` (plain JS props, unaffected by the native driver; the nav needs direction changes, not per-frame updates) + a mount/focus `setVisible(true)` reset + delete the dead import — REJECTED alternatives recorded: `scrollY.addListener` (reopens the per-frame JS round trip the native driver was chosen to avoid) and Reanimated `useAnimatedScrollHandler` (not a dependency); (2) split the bar into two curves — backdrop opaque by `[HERO_HEIGHT-100, HERO_HEIGHT-60]`, title/year on a later `[HERO_HEIGHT-50, HERO_HEIGHT]` — the widest-reaching fix since it fires on every title, not just the 37%; (3) padding 72/120 → 56/104, drop the year (flagged as a judgment call the user can veto, falls back to 30), then a deterministic 17→14px tier reusing the hero's length-tier shape NOT `adjustsFontSizeToFit` per [[custom-font-truncation-rescope]] — two-line bar explicitly NOT recommended (0% truncation but that is Material's *large* top app bar, wrong component); (4) derive the hero-fade constants from `HERO_HEIGHT` (no-op on this device, which is why it was never caught — ships with #2 or the new curves inherit the same latent bug); (5) `importantForAccessibility="no-hide-descendants"` toggled with visibility; (6) delete the dead iOS branches; (7) gate the mesh loop on focus — lowest user-facing priority, highest if the next run should be able to verify its own a11y work. Credit due: the floating pills land inside the collapsed bar band with matching clearance so it reads as one coherent top app bar; `reduceMotion` kills the parallax, the hero fade AND the mesh loop; everything visual is native-driven and jank-free (the bug in #1 exists BECAUSE the right performance call was made); `paddingBottom: insets.bottom + SCROLL_BOTTOM_PAD` [:1767](src/components/ResultView.js:1767) already gets the bottom-inset case right that three prior rail runs had to fix elsewhere; and the `heroContent` top-floor comment [:2718-2726](src/components/ResultView.js:2718) documents why the constraint exists. Suggested first pass = #1+#2+#4. **ALL 7 IMPLEMENTED and verified on the release APK on the A54 (R5CW31BDEMH); APK archived as `releases/trova-1.0.14-release-2026-07-27.apk`. 253/253 tests (3 new in `tests/title-meta.test.js`), 0 lint errors. ResultView warnings 48 -> 48: `no-unused-vars` 5 -> 4 (the dead `useBottomNavScroll` import deleted) and `react-hooks/refs` 38 -> 39 (one extra `scrollY.interpolate` at render, the identical pattern the file already trips 38 times); HEAD baseline confirmed by linting `git show HEAD:` through `eslint --stdin --stdin-filename` rather than overwriting the file.** New pure helper `stickyTitleFontSize` in `titleMeta.js`. **#1 device-verified in both directions AND on the original regression**: Home scrolled until the nav hid -> tap a poster -> the detail screen now opens WITH the nav (the pre-fix capture showed it stranded off-screen); on the detail screen a drag up hides it and a drag down brings it back, neither of which had ever worked. **#2 verified at the exact scroll offset that produced the two-titles-at-once screenshot** - the bar is now fully opaque there, nothing reads through it, and the label has not started to fade in. **#3 took TWO padding iterations on device and the recommendation was wrong the first time**: 56/104, the real button footprint the critique measured, put the first glyph about 1dp from the back circle - correct arithmetic, broken optics. Shipped 64/104 instead: 8dp of clearance on the left where a glyph would otherwise touch the pill, bare footprint on the right because a title that reaches that edge ends in an ellipsis whose dots supply their own optical gap. Title box 156 -> 216dp, clipped titles 37 -> 14 of 100; the 8dp cost 3 titles. Tier is `<=20 chars -> 18, else 15` pre-`scaleFont` (17dp/14dp here); a third tier at 13dp rescued only 3 more and was rejected as illegible in a top bar. **A MEASUREMENT LIMIT WORTH RECORDING: the TTF advance model runs about 3 percent wide.** It predicted "Law and Order: Special Victims Unit" at 223.9dp would clip in a 216dp box; on device it renders in full at 216.5dp. The model is good enough to rank options but not to call knife-edge titles - check those on screen. **#4 is a confirmed no-op on this device and that is the point**: measured `HERO_HEIGHT` here is 430dp (window about 756dp), exactly the literal 430 that was hardcoded, so the old constants lined up by coincidence; the fix is unverifiable on one screen height and is insurance for every other. **#5 VERIFIED ON DEVICE - the first accessibility tree captured from this app in six runs.** The only labelled nodes inside the 0-247px sticky-bar band are `Go back`, `Share ...` and `Manage ... in your library`; no sticky title, no year. The bare title node sits at bounds [68,802][791,1014], i.e. the hero logo, not the bar. **THE RECIPE, since five prior runs recorded this as impossible: `adb shell settings put global transition_animation_scale 0` - NOT `animator_duration_scale`, which does nothing here - makes RN `AccessibilityInfo.isReduceMotionEnabled()` true, so the mesh loop never starts and `uiautomator dump` succeeds; restore with `... transition_animation_scale 1`.** The same dump incidentally confirmed a11y labels that the info-pill and ratings-strip runs could only code-verify: `First aired 1999`, `28 seasons`, `Rated TV-14`, and `... TMDb 8.0 out of 10. Opens TMDb.` all present. **#6** deleted the `BlurView` branch and its import, `contentInsetAdjustmentBehavior`, the unreachable `-HERO_HEIGHT` pull-to-zoom leg, the redundant Android `absoluteFill` backdrop (the wrapper already carried the same `backgroundColor`), and the dead `fontWeight: 800`. **#7 proven by counterfactual, with animations ON**: a pushed Filmography screen over a mounted-but-unfocused ResultView dumps successfully, and going back to the focused detail screen fails with "could not get idle state" again - so the mesh loop is definitively what blocks idle, and the focus gate works. **My in-code comment initially overclaimed that #7 unblocks a11y auditing; it does not.** The loop legitimately runs on the visible screen, so the detail screen itself is still undumpable without the reduce-motion recipe above - what #7 fixes is the waste of animating behind every pushed screen. NOT verified: any window height other than 756dp (#4's entire purpose), and TalkBack's spoken output and focus order - the dump proves tree membership, not what is announced. Not committed - changes sit uncommitted on branch `feat/based-on-source-material`. |
| 2026-07-27 | Results / ResultView | Franchise / collection rail — numbered poster strip, "N films" badge, `/collection/{id}` fetch | Goodreads series page (primary-vs-total works, per-entry "expected publication", your position in the series); Apple TV / Prime Video pre-release badging as corroboration | A correct spine wearing a lying badge — B for the ordering, D for the honesty, F for a 27-film rail with no exit | Approved | Recommendations delivered (9, prioritized), then **ALL 9 IMPLEMENTED and verified on the release APK on the A54 (R5CW31BDEMH); APK archived as `releases/trova-1.0.15-release-2026-07-27.apk`; branch `feat/franchise-rail`. 278/278 tests (25 new in `tests/franchise.test.js`), 0 lint errors; ResultView warnings 48 -> 49, the one addition being a `react-hooks/set-state-in-effect` on the new post-paint effect — the identical line the rails effect already trips (HEAD baseline confirmed by linting `git show HEAD:` through `--stdin`).** Measured, not assumed: 100 popular movies (pages 1–5) + 60 top-rated + 9 mega-collection probes replayed through the app's own `sortCollectionParts`/`mapCollectionPart`. Findings: **32/100 popular movies render the rail**; **23 of 125 tiles (18.4%) are unreleased or undated** and **17/32 rails (53.1%) had a count badge overstating the films that exist** — Avatar "5 films" for 3, Avengers "6 films" for 4, plus placeholder entries literally titled "Untitled Michael Sequel" and "Untitled Avatar: The Last Airbender Film 2". **The damage concentrates in *currently popular* titles (12/32 rails) and nearly vanishes in settled franchises (2/20 top-rated)** — backwards, since popular titles are what users open. Device-proven payoff of a tap: Avatar 5 → a screen with no title above the fold, TMDb 0.0/10, "Plot TBA", and WHERE TO WATCH answering "Not free to stream anywhere right now" about a 2031 release. Second defect: no auto-scroll and no overflow screen — on **Skyfall** the rail opened on Dr. No (1962) under a "27 films" badge with Skyfall 24th, **2,993dp of swiping away**, while CAST directly below carried "See All 75". Also found: **four dead `fontWeight`s** (`franchiseTitle` 900, `franchiseCountText` 800, `franchiseYear` 700, `similarTitle` 700) — the third and fourth instance of the last-style-wins bug the rails run fixed on the score badge and the scroll-chrome run fixed on `stickyTitle`, plus two more in `seeAllText` and three in `FilmographyScreen`; the collection fetch was a **serial await inside `getTitleMetadata` ahead of `getProviderCountries`** (measured median 97ms / p90 114ms on 32% of movie detail screens); `franchiseKindForParts` was **dead code** (`franchiseKind` read nowhere, `franchiseLabel` only in a fallback that never fires — 52/52 sampled collections have a name); the mapped `belongsToCollection` object was built and read by nobody; and the section was the **only one on the screen not using `ProgrammeEyebrowLabel`**. Credit due, measured: tile geometry was already right (a11y dump measured 118.0dp tiles with a 75.7dp peek, matching the glyph/scale model to the pixel — the one rail in the file that never needed the `marginRight: -24` bleed); chronological sort is real work TMDb doesn't do; the tap re-hydrates through `resolveMatch` rather than pushing the partial object; posterless tiles degrade cleanly (2/125); no phantom skeleton; and the movie-only scope is a TMDb data limit, not a shortcut. **Checked rather than assumed — no cached-shape trap here**: `recentViewed` persists an explicit 9-field whitelist ([storage.js:210](src/lib/storage.js:210)) with no `collection`/`isFranchise`, so unlike the soundtrack and awards runs a fetch-time fix needed no presentation-time twin. Implementation: new pure module `src/lib/franchise.js` (`releaseStateFor`, `summarizeFranchise`, `franchiseCountLabel`, `franchiseTileMeta`, `franchiseTileA11yLabel`, `franchiseRailWindow`); new `fetchTitleCollection` post-paint entry point; `mapCollectionSeed` **repurposes** the object rec 8 said to delete — it rides `/movie/{id}` free and lets the header name the collection at first paint while the parts load, which is a better answer than deleting it; `FilmographyScreen` gained a `role: 'collection'` third case (the same reuse the production-companies run made for `role: 'company'`), threaded through all four nav stacks. **Re-measured the SAME 100-title sample through the shipped module: count badges overstating released films 17/32 → 0/32 (all 17 now disclose, e.g. Avatar "5 films" → "3 films · 2 upcoming", Avengers "6 films" → "4 films · 2 upcoming"); current title past the visible tiles at rest 6/32 → 0.** **THREE THINGS THE RECOMMENDATIONS GOT WRONG OR MISSED, all caught on the device, not by reasoning:** (1) *rec 3's "cap at ~10" was nearly a regression* — a head `slice(0, 10)` would have hidden Skyfall entirely, so `franchiseRailWindow` windows around the current title and each item carries `order` (its place in the FULL set) so badges stay honest when the window is offset; Skyfall's rail now reads 18–27 rather than 1–10. (2) **the auto-scroll took three attempts and only instrumentation found it.** First cut: the skeleton's `onContentSizeChange` spent the one-shot before any tile existed. Second: gating on content width still landed the rail one tile in. A `console.warn` proved the call was correct (`w=1329 count=10 idx=6 target=741`) and the scroll was simply being overwritten — **scrolling synchronously from inside `onContentSizeChange` is undone by the layout pass that follows it; deferring one `requestAnimationFrame` fixed it**, verified by a11y dump showing "24 of 27. Skyfall, 2012. Current title." with #23 peeking. (3) *the See-all grid's order badge collided with the rating badge* — both top-left; moved to bottom-left, the only free corner (rating top-left, bookmark top-right, media-type bottom-right). **Device-verified on 7 titles**: Avatar ("3 films · 2 upcoming"; tiles 4/5 dimmed with grey badges reading "2029 · Upcoming"/"2031 · Upcoming"); Skyfall (rail lands on Skyfall, "See All 27", "26 films · 1 upcoming"); the Bond See-all screen ("FILMS IN / JAMES BOND COLLECTION / 26 FILMS · 1 UPCOMING", 007 key art in a portrait plate, badges 1..27, **Bond 26 → muted badge + "Date TBA"**); Zootopia (the rail's unscheduled branch — Zootopia 3 dimmed, "Date TBA"); Parasite (standalone → **no Franchise section at all**, DETAILS straight to CAST); Breaking Bad (TV path unaffected); and the **error branch forced deliberately** with a temporary one-shot throw — the header still named "Avatar Collection" from the seed while the body read "Couldn't load this franchise. Tap to retry.", and tapping it loaded the rail. The temporary throw and the instrumentation were reverted and their absence confirmed in the shipped bundle. **a11y verified on device, not code-read** (`transition_animation_scale 0` recipe from the scroll-chrome run): tiles announce "18 of 27. GoldenEye, 1995. Open details." / "24 of 27. Skyfall, 2012. Current title." / "3 of 3. Zootopia 3, release date to be announced. Open details.", and the header group announces "James Bond Collection. 26 films · 1 upcoming." — where the old build said only "Open details for Dr. No". Bundle-checked rather than trusting a 33s "BUILD SUCCESSFUL / 757 up-to-date": extracted `assets/index.android.bundle` and confirmed `franchiseRailWindow`, `fetchTitleCollection`, "Date TBA", "Films In" present and `franchiseKind`, `belongsToCollection`, `franchiseCountBadge`, `TEMP_FAIL` absent. Explicitly NOT done, as recommended: per-tile availability badges (27 provider lookups for one rail) and filtering unreleased parts out. Left for later: the same last-style-wins `fontWeight` bug is still live elsewhere in the app — worth the repo-wide grep rec 5 called for. Not committed — changes sit uncommitted on `feat/franchise-rail` | Shipped — all 9 verified on device (uncommitted) |
