# Feature Critique — ledger

One row per critique run. Read this before picking a target (skip anything listed
here unless the user points at it on purpose); append to it at the end of every
run, including rejected critiques.

| Date | View | Feature | Compared to | Verdict | Decision | Outcome |
|------|------|---------|-------------|---------|----------|---------|
| 2026-07-23 | Results / ResultView | Bookmark → save to collections | Spotify add-to-playlist sheet | B− flow wearing an A-tier feature set | Adjusted — "user must pick a destination every time" | Shipped — PR #68 |
| 2026-07-23 | Results / ResultView | Where To Watch availability table | JustWatch title-detail "Where to watch" | C+ presentation wrapped around an A− dataset | Adjusted — global atlas is the point (don't localise it away); no deep links (users just want to know which country has it); free-to-watch only is deliberate, rent/buy stays excluded | All 8 shipped — PR #70, verified on release APK on the A54 |
| 2026-07-23 | Results / ResultView | Cast & Crew rail (grouping, See All overflow, long-press peek sheet) | IMDb title page "Top cast" + "Cast & crew" full page | A-tier cards on a rail with no information architecture — B for any single card, D for everything past card ten | Approved | All 8 shipped — PR #71, verified on release APK on the A54; user chose full-cast screen for #2 and left #5's affordance to me (persistent corner badge) |
| 2026-07-23 | Results / ResultView | Seasons rail (TV titles) — inert cards, no header, no season-scoped availability | Apple TV app series page (season selector + per-season provider attribution) | A-tier data pipeline feeding a rail of coasters — B+ for the fetch, D for the section | Approved | All 7 shipped — verified on release APK on the A54 (Squid Game 3-season, Chernobyl 1-season, Parasite movie regression). Core fix: `getCompleteTvProviderCountries` now folds the same episode fan-out twice (per-season, then across seasons) instead of intersecting season granularity away. Per-season availability needs `EXPO_PUBLIC_TMDB_TV_EPISODE_LOOKUP=true`; the sheet degrades to a fallback line when off |
| 2026-07-24 | Results / ResultView | Synopsis expand/collapse ("read more") — manual `substring(0,250)` truncation, whole paragraph as invisible toggle | Letterboxd film-page synopsis "…more" clamp (Apple TV detail-page "…MORE" as big-app analog) | A-tier data plumbing wrapped around a C− interaction that looks broken on every long synopsis | Approved | All 4 implemented and verified on release APK on the A54 (device R5CW31BDEMH). (1) `numberOfLines={4}` line clamp replaces the `substring(0,250)` slice — native ellipsis breaks at the column edge, no more mid-word cut; (2) gold "Read more / Read less" toggle gated on measured overflow — the first paint renders unclamped so `onTextLayout` counts true lines, then clamps (flash happens offscreen below the 480px hero); (3) a11y — the overflow branch is a real `accessibilityRole="button"` carrying the full synopsis as its `accessibilityLabel` + `accessibilityState={{ expanded }}`, so TalkBack no longer reads the truncated fragment; (4) tap-anywhere retained atop the visible link. Verified round-trip on The Hangover (clamped→Read more→full text ending cleanly at "church on time."→Read less→re-clamped); short synopses show no toggle. 154/154 tests green, 0 lint errors. Not committed — changes sit uncommitted on branch `fix/share-card-country-picker`. a11y props code-confirmed on the now-live button branch; couldn't `uiautomator dump` (the mesh-orb loop blocks idle state). |
| 2026-07-23 | Results / ResultView | Share Card flow (share button → country picker sheet → off-screen ViewShot capture → OS share) | Spotify share sheet on a track/album (live card preview, per-destination formats, live `spotify.link`) | B for the artifact, D for the flow, F for the payoff — a great card, generated blind, timed by a stopwatch, carrying a QR code to nowhere | Approved | All 7 implemented and verified on release APK on the A54. QR payload is now the TMDb title URL (user's call); this forced a QR encoder bump from version 2-L to 3-L, since v2 held 32 bytes and `qrMatrix.js` silently truncated — a 40-byte TMDb URL would have scanned to a broken address. New `tests/share-card.test.js` decodes the matrix back the way a scanner does (round-trip proof), 151/151 suite green. Deep links needed `getTitleMatchById` (a bare `{tmdbId, mediaType}` rendered a detail screen titled "undefined") plus a ref-held callback in `useDeepLink` — `openResolvedDetail`'s identity changes when `recentViewed` loads, which was tearing down the in-flight handler and dropping ~1 in 3 cold starts; now 4/4. Three bugs surfaced only on device: the story cap desynced `imageUris` from rendered images so readiness never armed; React reconciles the two card layouts positionally and reuses loaded `<Image>`s (fixed with `key={format}`); and the preview measured its height inside the constrained container, cropping instead of scaling. Not exercised on device: the disabled-CTA empty-selection state (logic unit-tested only) |
| 2026-07-24 | Results / ResultView | Hero content title — fixed 56px, no `numberOfLines`/auto-shrink, bottom-anchored stack grows upward on long titles | Apple TV app detail-page title treatment (logo-art-first + scaling text fallback); Netflix logo-art as corroboration | A-tier typography with zero fault tolerance — a magazine cover that assumes every title is one word | Approved | Recommendations delivered (4, prioritized). Core finding: the uncapped bottom-anchored title ([ResultView.js:1302](src/components/ResultView.js:1302), heroContent [:2305]) is the ROOT CAUSE of the "Metacritic badge under the floating buttons on long titles" overlap that the ratings-strip run deferred — fixing title growth retires it for free. Recs: (1) `numberOfLines={3}` + reserved band / floor above the floating header; (2) `adjustsFontSizeToFit` minScale ~0.6 (release-APK verify, Android+custom-font flaky — measured-length tier as fallback); (3) neutralize `letterSpacing:-2` glyph-clip via token relax and/or `alignSelf:'stretch'`+`textAlign` per [[custom-font-truncation-rescope]]; (4) title-treatment logo art (TMDb `images.logos`, not fetched today) flagged as SEPARATE scope. First-pass scope = #1+#2+#3. **All 4 implemented and verified on release APK on the A54 (R5CW31BDEMH), branch `fix/ratings-strip-fidelity` (uncommitted).** #4: `getTitleMetadata` now appends `images` with `include_image_language=en,null`; new `pickTitleLogo` picks best raster (non-SVG, en>null>other, then votes) → `result.titleLogo {url, aspectRatio}`; ResultView renders logo-first (`ExpoImage`, contain/left, sized into a verticalScale(84) band, `onError`→text fallback). D&D shows the official title logo. #1: `heroContent` gained a `top` floor (`insets.top+52`) + `justifyContent:'flex-end'` + `overflow:'hidden'` so the bottom-anchored stack can't draw over the floating share/bookmark buttons — retires the deferred ratings-strip overlap. #2: deterministic length tiers (≤16→56px, ≤40→34px, else 28px) replace `adjustsFontSizeToFit` — device test proved `numberOfLines={3}` alone doesn't bound *height*, so 3 full-size lines clipped the whole meta stack; tiers shrink long titles (D&D→34px/2 lines) so the ratings strip survives. #3: `letterSpacing:-2`→`-1` + `alignSelf:'stretch'`+`textAlign:'left'` — no last-glyph clip on "Thieves"/"Moon"/"Barbie". Verified 4 cases: D&D logo path (clean); D&D forced-text (34px, full title, meta intact); Killers of the Flower Moon (soundtrack+trailer worst case — ratings strip preserved, no clip); Barbie (short, full 56px). 160/160 tests, 0 lint errors. Meta-stack margins tightened (marginBottom 20→12, gap 12→10) to keep ratings clear even with a soundtrack button. Spotted but OUT OF SCOPE: Killers' soundtrack button renders a raw Wikidata QID ("Q123137009") instead of a track name — pre-existing soundtrack-feature bug |
| 2026-07-24 | Results / ResultView | Hero artwork selection — which image sits behind the title treatment; `backdropUrl \|\| posterUrl` takes TMDb's community-voted primary, double-titling now that we render our own logo/text | Apple TV / Netflix "background art vs title art" split (textless background mandated so the overlaid title never collides) | Picks the wrong *kind* of art, not the wrong language — TMDb's crowd-pleaser key-art is the one image guaranteed to fight a title treatment | Approved (after user supplied LOTR + Portrait examples that disproved the first-pass "filter iso_639_1:null" fix) | Recommendations delivered (4, prioritized). Diagnosis corrected mid-gate via device evidence: deep-linked LOTR Fellowship (id 120) + Portrait of a Lady on Fire (id 531428) on the release APK — LOTR's on-screen backdrop IS `iso_639_1:null` yet is ornate Elvish-script key-art, proving TMDb's `null` tag = "no English *title* text", NOT "clean still". Two failure modes: (1) literal double-title — poster fallback (poster always has title; ~11% of low-popularity tail has NO backdrop → poster) + mistagged backdrops; (2) key-art clutter — TMDb community upvotes dramatic promo art as `backdrop_path` primary, which is exactly what `heroArtUri` grabs ([ResultView.js:1040](src/components/ResultView.js:1040)). Measured: 50 popular movies + 44 popular TV = 0 with-text/0 no-backdrop (mainstream already clean); problem is entirely the obscure/foreign/TV tail. Recs: (1) `pickHeroBackdrop(images.backdrops)` selector mirroring `pickTitleLogo` — `null`-only, prefer ~1.778 aspect + resolution, do NOT rank by votes (votes surface the key-art), fallback to old `backdrop_path`; honest ceiling — heuristic, TMDb has no "clean still" flag. (2) Suppress our title overlay when the hero falls back to the poster ([ResultView.js:1206-1209](src/components/ResultView.js:1206)) — deterministic double-title kill. (3) Title-legibility scrim behind the logo/text (extend the existing bottom scrim [:1179-1186]) — robust catch-all independent of TMDb data quality. (4) Prereq: unify where `backdropUrl` is set (search [tmdb.js:262/:317], `getTitleMatchById`, cached recentViewed) — found the on-device Portrait backdrop ≠ detail-endpoint `backdrop_path`, so selector must live where all paths converge (`getTitleMetadata`/`resolveMatch`). `images` already fetched for logos, so selection is free. First-pass scope = #1+#2+#3; verify on LOTR/Portrait/poster-fallback/2 mainstream. **Implemented + verified on release APK on the A54 (R5CW31BDEMH), branch `fix/ratings-strip-fidelity` (uncommitted).** #1 `pickHeroBackdrop` in tmdb.js → `result.heroBackdropUrl`, consumed first in ResultView (`heroBackdropUrl || backdropUrl || posterUrl`). KEY LESSON from device: the selector must rank the textless (`iso_639_1==null`) pool by **community vote_count** (with a ~16:9 preference, resolution tiebreak), NOT by aspect/resolution alone — a first cut ranking width-first regressed D&D to a dark still; votes are the hero-quality signal, and the ornate key-art that fights the title (LOTR ring inscription) is TMDb's designated *primary* but NOT top-voted-null, so votes both keep great defaults (D&D warm cast shot) and step around the key-art. #2 title overlay (logo/text) suppressed when `usingPoster` (no backdrop of any kind) so a title-bearing poster isn't double-titled. #4 selector lives in `getTitleMetadata` where all resolveMatch paths converge. **#3 (title-legibility scrim) BUILT THEN REMOVED**: first impl was a `StyleSheet.absoluteFill` LinearGradient INSIDE the inset `heroContent` (left/right:24, overflow:hidden) → rendered a hard-edged dark rectangle with a visible vertical "cut-off" line, glaring on Barbie's bright sky; user flagged it. Removed — #1's clean-still selection makes it redundant and logos/text stay legible without it (verified LOTR gold logo + Portrait white handwritten logo on their stills). Verified 5 heroes on device: LOTR (ring-art→clean hobbits still), Portrait (→clean beach still), D&D (→warm cast still, no dark regression, no line), Barbie (→bright still, no dark box), Vampire Princess Miyu id 31649 (no backdrop → poster shown, our title suppressed, no double-title). 160/160 tests, 0 lint errors |
| 2026-07-25 | Results / ResultView | Watch Trailer — trailer selection (`getTitleMetadata`), the `TrailerModal` popup, auto-rotate, and age-restricted playback | YouTube Android app's watch-page player (rotate-to-fullscreen, typed error surfaces, signed-in age gate) | A correctly-plumbed YouTube embed with every failure path amputated — A− on the happy path, F on everything else | Approved | Recommendations delivered (6, prioritized). **Measured, not assumed** — 120 popular titles (movie+TV pages 1–3) run through the real picker ([tmdb.js:470-474](src/lib/tmdb.js:470)), each picked YouTube key probed for `playabilityStatus`: 29/120 (24%) render NO button; of the 91 with a button, 66 official Trailer / 19 unofficial / **6 not a trailer at all** (Mentalist→"Behind the Scenes: The Designers", SVU→featurette, Letterman→opening credits) and **4/91 (4.4%) unplayable in an embed** — Vixen! `FuMIkgbiyGI` = `LOGIN_REQUIRED` "Sign in to confirm your age" (the live age-gate case), Deep In `HBwTZENixTE` geo-blocked, Tonight Show `zyl4Vvjc7pI` copyright-removed, Sparrow's Fluttering content-claimed. **None fire `onError`** — the WebView loads HTTP 200 and YouTube renders its own error inside, so the spinner clears ([:163](src/components/TrailerModal.js:163)) over a permanent black rectangle. Opening hypothesis CORRECTED by data: `language=en-US` does NOT starve popular foreign titles (Parasite/Squid Game/AoT all resolve, though pools shrink 18→4); the damage is entirely in the tail — 8 of the 29 buttonless titles are rescued purely by adding `include_video_language`. Rotation finding: NOT fully broken — `react-native-webview` sets `SCREEN_ORIENTATION_UNSPECIFIED` on fullscreen ([RNCWebViewManagerImpl.kt:160](node_modules/react-native-webview/android/src/main/java/com/reactnativecommunity/webview/RNCWebViewManagerImpl.kt:160)), overriding the manifest portrait lock at runtime; but it's reachable ONLY via YouTube's own tiny fullscreen icon, and the modal itself never rotates. Recs: (1) load an IFrame-Player-API host page (`source={{html, baseUrl:'https://www.youtube.com'}}`) instead of the embed URI so error codes 101/150/100/2/5 become visible — the prerequisite for everything else; keep the hand-tuned UA [:20] + `Referer` [:151] and fall back to the direct URI if the bridge doesn't `onReady`; (2) restore the DEAD `errorBox`/`errorText`/`errorAction` styles ([:259-278](src/components/TrailerModal.js:259), defined and never rendered) and delete the toast-and-auto-close ([:169](src/components/TrailerModal.js:169), [:82-87](src/components/TrailerModal.js:82)) — age-gate copy hands off to YouTube via existing `handleOpenYouTube` [:89] (an embed can NEVER play age-restricted; handoff is the only honest fix); (3) picker — add `include_video_language`, replace the `youtubeVideos[0]` fallthrough with tiers (official Trailer→official Teaser→any Trailer→any Teaser→nothing), carry `trailerType` so the button can say "Watch Teaser", and (3d, depends on #1) return the ranked list so a dead video auto-advances; (4) `expo-screen-orientation` (not a dep today) — `unlockAsync()` NOT `OrientationLock.ALL` so the system auto-rotate toggle is honored; restore in cleanup not just `handleClose`; **ordering trap**: unlock BEFORE the WebView mounts or its `initialRequestedOrientation` snapshot ([:146](node_modules/react-native-webview/android/src/main/java/com/reactnativecommunity/webview/RNCWebViewManagerImpl.kt:146)) re-locks portrait on fullscreen exit; manifest already has `configChanges=orientation\|screenSize` so no activity recreate; leave `app.json:7` portrait; (5) look & feel — force dark chrome (`colors.surface` is `#ffffff` in light mode → white header bar + full-screen white flash), own the StatusBar (dark icons on the `#000` inset strip make the clock/battery vanish), 16:9 frame instead of `flex:1` letterboxing into a black void, move the YouTube pill into the header, swipe-to-dismiss, `fade` not `slide`; (6) `heroBackdropUrl` as poster frame behind the loader. Credit due: the Android UA spoof + `Referer` + "don't add `origin=`" comment are exactly right and why 87/91 play flawlessly. First-pass scope = #1+#2+#3+#4 (+#5 rides along). **ALL 6 IMPLEMENTED and verified on the release APK on the A54 (R5CW31BDEMH), branch `feat/detail-hero-polish` (uncommitted); APK archived as `releases/trova-1.0.7-release-2026-07-25.apk`. 168/168 tests (new `tests/trailer-picker.test.js`, 8 cases), 0 lint errors/0 warnings in the touched files.** New `src/lib/trailerPicker.js` holds `rankTrailerCandidates` (pure, unit-tested): tiers official Trailer→official Teaser→any Trailer→any Teaser→**nothing**, then English→null→other, then newest `published_at`, capped at 5 candidates; `tmdb.js` gained `include_video_language: 'en,null'` on the main call plus a one-extra-request original-language sweep fired only when the tiered pick is empty, and returns `trailerType` + `trailerCandidates`. **KEY LESSON — Rec #1 as specified was WRONG and device testing caught it**: serving an IFrame-Player-API host page via `{html, baseUrl:'https://www.youtube.com'}` does NOT give the document a real youtube.com origin on Android (`loadDataWithBaseURL` leaves it opaque), so YouTube answered **every** video with error 152 — the exact trap the original `origin=` comment warned about. Proof: instrumented the error code, then all five Blade Runner 2049 candidates failed 152 in sequence. Vixen! showing an error state on the first build was a FALSE POSITIVE (right UI, wrong reason). Fix: keep the proven direct `/embed/` URI as the document and observe the player *from inside it* via `injectedJavaScript` — same-origin, so it polls `.ytp-error` (refusal) and `movie_player.getPlayerState()` 1/3 (picture on screen). Reading YouTube's own refusal **text** beats the numeric codes: it separates an age gate from a region block, which 101/150 collapse together. Device-verified: Vixen! → text "This video is age-restricted and only available on YouTube" → age branch ("This trailer is age-restricted… Open it in YouTube, where you're signed in") + gold CTA; FROM → candidate #1 `78tTuVoKONg` geo-blocked ("uploader has not made this video available in your country") → **auto-advanced silently to #2, which played** (user saw no error); The Mentalist → button GONE (its featurette no longer masquerades as a trailer); Jana Nayagan (ta) and Kung Fu Soccer (zh) → button now present and playing; Blade Runner 2049 + Arcane → play clean, no refusals. Rotation: `expo-screen-orientation` added; `unlockAsync()` (NOT `OrientationLock.ALL`, which would override a user who turned auto-rotate off) before the WebView mounts, `lockAsync(PORTRAIT_UP)` in the effect cleanup. Device-verified with auto-rotate OFF: forcing `user_rotation 1` rotated the app mid-playback (`cur=2340x1080`, header dropped, floating close top-left, no reload thanks to the existing `configChanges`), and backing out restored `cur=1080x2340` even with `user_rotation` still 1. Chrome: hardcoded player palette — light mode verified showing a dark header and no white flash (was `colors.surface`=`#ffffff`). Also: 16:9 `aspectRatio` stage replaces `flex:1` letterboxing, YouTube pill moved into the header, `heroBackdropUrl` poster behind the loader, swipe-down dismiss via plain responder props (a `PanResponder` tripped the `react-hooks` refs-during-render rule), `fade` not `slide`, dead `errorBox` styles finally rendered, toast-and-auto-close deleted. **Re-measured the original 120-title sample against the new picker**: non-trailer buttons 6→**0**; 7 titles rescued by `include_video_language`; 1 saved by auto-advance; 4 genuinely unplayable now get an explained error state instead of a black rectangle. Net "no button" 29→28 — composition changed, not just the count: +7 real trailers gained, ~6 lying buttons removed. A `console.warn` on refusal was kept deliberately — it's the instrumentation that caught the 152 bug and the failure class is otherwise invisible. Not verified on device: the no-candidates-at-all empty state (no sampled title reached it), and the status bar stays visible in landscape despite `hidden={isLandscape}` (cosmetic, left as-is). **FOLLOW-UP ROUND — user reported 3 defects after first delivery, all fixed and re-verified.** (a) *Close and YouTube buttons felt unresponsive* — self-inflicted: the header's `onMoveShouldSetResponder={() => touches.length === 1}` claimed the responder on ANY movement, so a tap with a pixel of finger drift stole it from the child buttons and cancelled the press. Fixed by recording the touch origin in `onStartShouldSetResponderCapture` (returns false, never claims) and only claiming on `dy > 15 && dy > |dx|`. Device-verified: close dismissed within 900ms of tap; YouTube pill had `com.google.android.youtube` topResumedActivity within 1.2s. (b) *Landscape didn't fit the screen* — root cause found by instrumenting both sides: RN was correct (`frame layout 832 384`, stage `{x:0,y:0,w:832,h:384}`) but the WebView reported a **909x420dp viewport inside an 832x384dp view** — it caches the display density it was created with and MainActivity's `configChanges` (the very thing that keeps playback alive) means the activity never recreates to refresh it, so the page lays out at ~412dpi instead of the A54's 450 and YouTube sized `#movie_player` to 832 *CSS* px offset by 29. Fix: inject a stylesheet pinning `html,body,#movie_player,.html5-video-player` to 100%/left:0/top:0 so the miscomputed pixel size stops mattering. (c) Also added a `useLayout`-measured `frame` (onLayout on the modal container) because `useWindowDimensions` reports the *activity* window (755.55dp) not the modal's (832dp). **Two attempted fixes were built, measured, and REVERTED as provably useless**: `movie_player.setSize(innerWidth, innerHeight)` and remounting the WebView keyed on orientation (with `start=` resume) — screenshots at each step were geometrically identical to the CSS-only fix, so the remount was removed and playback again survives rotation with no reload. **Also corrected a false alarm**: the remaining 210px-per-side black bars in landscape are CORRECT — Arcane/Blade Runner trailers are 16:9 uploads (scope image with bars baked in), so 1920x1080 centred in a 2340x1080 screen is exactly right; I had briefly mistaken this for residual mis-sizing. Gotcha worth remembering: `PLAYER_OBSERVER_JS` is a template literal, so a backtick in a comment inside it (I wrote \`configChanges\`) silently breaks the Metro bundle with a parse error at build time, not lint time |
| 2026-07-25 | Results / ResultView | Spotify soundtrack CTA — gold button under Watch Trailer, `Choose Soundtrack (N)` picker sheet, Wikidata `P406→P2205` → Spotify handoff | Google movie knowledge-panel soundtrack module (a non-music product surfacing a film's music from a third-party knowledge graph — same job, same failure class) | A-tier plumbing wired to a button that can't say what it does — B+ for the handoff, D for the label, F for the 17.5% that ship a database key as a call to action | Approved | Recommendations delivered (5, prioritized), no code written. **Measured live against Wikidata, not assumed** — ran the app's own query shape ([ResultView.js:237](src/components/ResultView.js:237)): 953 bindings → **800 titles worldwide** can ever render this button (750 single / 50 multi), 871 releases. Defects quantified: **140/800 titles (17.5%) render a gold CTA whose entire text is a bare QID** — `parseSoundtracksFromBindings` guards against the entity *URI* form ([wikidataSoundtracks.js:133](src/lib/wikidataSoundtracks.js:133)) but `wikibase:label` returns a bare `"Q123137009"` string for unlabelled entities, which sails through; confirmed on the ledger's own example `tt5537002` (Killers of the Flower Moon) → `soundtrackLabel = "Q123137009"`. **862/871 releases (99.0%) have no `P18` cover**, so the picker's 56px art column is a musical-note glyph by default, not by fallback ([SoundtrackPickerSheet.js:43](src/components/SoundtrackPickerSheet.js:43)); and **64% of multi-release sheets share one year across every row** (Frankenweenie both 2012, Samurai Champloo all four 2004, 8 Mile both 2002), so the only disambiguator is inert. Root cause named: using the Wikidata album title *as* the button text ([ResultView.js:1427](src/components/ResultView.js:1427)) — every other defect is downstream (QID leak is only visible because the label is the CTA; truncation only matters because a 63-char *"Mad Max Beyond Thunderdome – Original Motion Picture Soundtrack"* is load-bearing in a `numberOfLines={1}` pill; the verb-less gold twin of Watch Trailer only exists because the entity took the action's slot — both use `styles.trailerButton` + `GOLD_ACCENT`). Recs: (1) fixed action label ("Soundtrack"/"Play Soundtrack"), album demoted to sheet content — recommended variant is *always* open the sheet incl. single-release (shows which album before throwing the user into Spotify, collapses two paths, matches [[save-flow-pick-every-time]]); also de-emphasize from primary since two full-width gold primaries stacked is one too many; (2) `/^Q\d+$/` reject in the shared parser — **keep** QID-labelled releases (they're playable, just nameless) and label the row "Untitled release" rather than dropping 140 titles; put the check in the parser not the render site since `basedOn` [:289](src/components/ResultView.js:289), awards, languages and countries all use the same `isEntityUri` idiom and share the exposure (leak rate on those paths NOT measured — worth a look); (3) route covers through the existing `resolveCommonsThumbUrls(values, 250)` ([:54](src/lib/wikidataSoundtracks.js:54)) which is exported, unit-tested and already used by awards ([wikidataAwards.js:124](src/lib/wikidataAwards.js:124)) — but capped payoff, only 9/871 releases (1.0%) have a cover at all; (4) picker rows should lead with the distinguishing fragment (strip the redundant leading film title, drop the year line when all rows share it) — deferred, only pays off on 50 titles, and a second SPARQL arm for track count/artist doesn't earn its keep; (5) delete the `wikiLoading` skeleton pill ([:1405-1414](src/components/ResultView.js:1405)) — it reserves button-shaped space on *every* detail screen and collapses on ~99.9% of them; leave the awards skeleton [:2026](src/components/ResultView.js:2026) alone, its odds are far better. **Withdrew critique point #6 (search fallback) at recommendation time**: the `spotifyAlbumId` hard gate ([:130](src/lib/wikidataSoundtracks.js:130)) is the same discipline the trailer feature had to be retrofitted with after shipping 4.4% unplayable buttons — a `?q=<title> soundtrack` link would put a gold CTA on every title while guaranteeing nothing on the other end. Credit due: `openSpotifyAlbum` ([spotify.js:6-26](src/lib/spotify.js:6)) is exemplary (native scheme → `canOpenURL` guard → try/catch → web player, no dead ends); dedupe-by-QID is load-bearing (953→871); the feature is free, riding the Wikidata call already in flight for `basedOn`/awards; and no mainstream streaming-finder links a film to its soundtrack album at all. Suggested first pass = #1+#2 | **ALL 5 IMPLEMENTED and verified on the release APK on the A54 (R5CW31BDEMH), branch `feat/trailer-playback` (uncommitted); APK archived as `releases/trova-1.0.8-release-2026-07-25.apk`. 179/179 tests (11 new in `tests/wikidata-soundtracks.test.js`), 0 lint errors and no new warnings (46 pre-existing mesh-orb `react-hooks/refs` warnings, identical count at HEAD).** User took both open calls my way: #1 always opens the sheet, #2 keeps QID-labelled releases as "Untitled release". New pure helpers in `wikidataSoundtracks.js`: `isBareQid`, `labelOrNull`, `stripLeadingTitle`, `buildSoundtrackRows`, `resolveSoundtrackCovers`. Button is now a fixed "Soundtrack" / "Soundtrack (N)" in a new `soundtrackButton` style — gold tonal outline (`GOLD_DIM` border, 10% gold fill, `GOLD_ACCENT` text/icon) instead of a second solid-gold primary competing with Watch Trailer. `parseSoundtracksFromBindings` keeps unlabelled-but-playable releases with `title: null`; `sortSoundtracks` made null-safe and sorts unnamed last. **GAP CAUGHT DURING IMPLEMENTATION, not in the recommendations**: watchlist entries enriched by an older build still hold a bare QID in AsyncStorage and reach the UI via the cached branch ([ResultView.js:517-524](src/components/ResultView.js:517)) *without* being re-parsed, so the parser-level guard alone would still have leaked QIDs for saved titles — `buildSoundtrackRows` therefore re-runs `labelOrNull` at presentation time (covered by its own test). Device-verified: **Killers of the Flower Moon → "Soundtrack" button, sheet row "Untitled release / 2023"** (was a gold CTA reading "Q123137009"); tapping through opened `https://open.spotify.com/album/43MjCtL4DwTxc1DhXK9Kpp` — exactly the id Wikidata holds — via the web-player fallback, since Spotify isn't installed on this device, confirming `openSpotifyAlbum`'s native→web path intact. **Frankenweenie (62214) → "Soundtrack (2)" → rows "Original Motion Picture Soundtrack" + "Unleashed! – Music Inspired by the Motion Picture", no year lines** (both 2012, uniform → hidden). **Yellow Submarine (12105) → rows "Yellow Submarine / 1969" + "Songtrack / 1999"** — years shown because they differ, and row 1 keeps its full name because stripping would empty it. Both branches of the year rule and both branches of `stripLeadingTitle` verified on device. **Blade Runner 2049 (335984) mid-load: no shimmer bar between Watch Trailer and the info pills at 3s/5s/13s** — #5 confirmed, layout stable from first paint. #3 (thumbnail resolver) is unit-tested only: none of the device titles carry `P18` art, consistent with the measured 99.0% — not visually confirmed, and by design it can only ever affect ~1% of releases. Regression-checked the sibling label paths I touched: a live replay of the new `labelOrNull` handling over 6 adaptations (LOTR Fellowship, Requiem, KOTFM, The Dark Knight, Breaking Bad, Barbie) dropped **0** rows, and Requiem for a Dream renders "BASED ON — Literary work: Requiem for a Dream by Hubert Selby Jr." plus intact Cast/Crew/Awards on device. Two gotchas for next time: the app's package is **`kkadogo.findstreamer.com`**, NOT `com.jeanm.trova` — force-stopping the wrong name silently no-ops and leaves a stale sheet mounted over a deep-linked screen (this briefly looked like a stale-content bug and was purely a test artifact); and `uiautomator dump` still fails here (mesh-orb loop blocks idle), so slow `input swipe … 900` drags are needed instead of default flings, which overshoot whole sections | Shipped — all 5 verified on device (uncommitted) |
| 2026-07-25 | Results / ResultView | Bottom recommendation rails — "More Like This" (`getSimilar`) and "More From Cast & Crew" (`getMoreFromCastAndCrew`) | Netflix "More Like This" title-page row (Apple TV person-scoped "More From <name>" rails as corroboration on attribution) | A rail that answers "what next" with a 1-vote film, and a rail that answers "who else" without saying who — B for the plumbing, D for the ranking, F for the labelling | Approved | Recommendations delivered (6, prioritized), no code written. **Measured against live TMDb via the app's own logic** — 100 popular titles (60 movie pages 1–3 / 40 TV pages 1–2), plus a 5-title latency run. *More Like This*: `/similar` and `/recommendations` overlap **1.0%** — effectively disjoint datasets, and the app picked the keyword one while [`fetchSurpriseRecommendation`](src/lib/tmdb.js:743) in the same file already calls `/recommendations`. The `vote_average` re-sort at [tmdb.js:726](src/lib/tmdb.js:726) has **no vote-count floor**: **51.8% of shown picks have under 200 votes**, median vote count of an on-screen card is **168**, and the 5 cards sit on average **9.2nd** in TMDb's own relevance order (the sort actively discards the most-similar items). Real output — The Odyssey → *LANDEaD* **10.0 from 1 vote**; Moana → *Les Franglaises – Le Viens-Retour* **9.0 from 2 votes**. Also **15/450 picks have no poster** and still render (sibling function filters this at [:1409](src/lib/tmdb.js:1409)); 10/100 titles return an empty `/similar`. *More From Cast & Crew*: `with_people=a\|b\|c` OR-join ([:1400](src/lib/tmdb.js:1400)) + `vote_average.desc` + `vote_count.gte:100` is a blockbuster magnet — **Avengers: Infinity War appears on 11 of 100 title pages**, Endgame 9, The Dark Knight 5, Across the Spider-Verse 5 — and the connection is structurally invisible (*Backrooms* → **12 Years a Slave** via Chiwetel Ejiofor; *Minions & Monsters* → **Inglourious Basterds**, **The Help**). `/discover/movie` is hardcoded at [:1399](src/lib/tmdb.js:1399) so **33/40 TV pages render a movies-only rail** under an unqualified header. Both rails gate first paint — `getSimilar` is a serial await ahead of `getProviderCountries` ([tmdb.js:1490-1491](src/lib/tmdb.js:1490)) and the cast/crew rail fires **26 requests per detail screen** (credits + discover + 12 `external_ids` + 12 OMDb via [`enrichDiscoverResults`](src/lib/tmdb.js:1075)), measured **~800ms for the TMDb leg alone**, OMDb on top. Render defects: both rails share `styles.similarRating` [:2859](src/components/ResultView.js:2859) but it means IMDb on one ([:2208-2214](src/components/ResultView.js:2208), hardcoded `#F5C518` + inline `fontSize:10` bypassing typography tokens) and a bare unlabelled number on the other ([:2256-2260](src/components/ResultView.js:2256)); neither shows a year; a11y label is `Open details for ${title}` on both. Recs: (1) vote-count floor + drop the rating re-sort in `getSimilar` (reuse the `voteCount >= 75` shape already at [tmdb.js:779](src/lib/tmdb.js:779); `vote_count` isn't currently captured in the map); (2) `/similar` → `/recommendations`, lands in the same 3 lines as #1; (3) **replace `/discover/movie` with per-person `/person/{id}/combined_credits`** for the top 3–4 people — attribution becomes free (the OR-join structurally cannot say which person matched), kills the blockbuster attractor, returns TV so the movies-on-TV-pages bug retires for free, and cuts 26 requests to 4; knowingly loses the IMDb re-sort at [:1424-1430](src/lib/tmdb.js:1424) (re-add on the final 5 only if wanted, 5 requests not 24); (4) defer both rails off the critical path, mirroring the existing Wikidata post-paint + skeleton + retry pattern in this same component ([ResultView.js:631](src/components/ResultView.js:631), [:2045](src/components/ResultView.js:2045)); (5) extract one shared rail card with year + source-labelled score reusing `ScoreValue` [:369](src/components/ResultView.js:369) — the franchise rail [:1859-1875](src/components/ResultView.js:1859) is already the target pattern in-file; (6) `poster_path` filter + re-measure the empty state after #2. Credit due: **zero cross-rail duplicates across 100 titles** (measured); the OMDb IMDb re-sort is the right instinct in the wrong place; the cast/crew rail *does* filter posterless (the asymmetry is the bug, not this half); `onSelectSimilar` wired identically across all 4 nav stacks with correct `mediaType` passthrough; and the OR-join genuinely lands when the person pool is director-dominated (The Odyssey → The Dark Knight, Interstellar, Inception). **Open decision left with the user**: one merged rail with per-card attribution (my default, less layout work) vs Apple-TV-style one rail per person. Suggested first pass = #1+#2, then #3+#4 together | **ALL 6 IMPLEMENTED and verified on the release APK on the A54 (R5CW31BDEMH); APK archived as `releases/trova-1.0.9-release-2026-07-25.apk`. 195/195 tests (16 new in `tests/rail-picker.test.js`), 0 lint errors; ResultView warnings 46→47, the one addition being `react-hooks/set-state-in-effect` in the new rails effect — the identical pattern the existing Wikidata effect already uses (HEAD baseline confirmed by linting `git show HEAD:` output).** I took both open calls my way (merged rail with per-card attribution; OMDb re-sort dropped). New pure module `src/lib/railPicker.js` (`rankSimilarTitles`, `selectRailPeople`, `creditsForPerson`, `rankPeopleTitles`, `railScore`) + new `src/components/TitleRailCard.js`; `fetchTitleRails` in tmdb.js is the single post-paint entry point. **Re-measured the SAME 100-title sample through the shipped module: cards under 200 votes 51.8% → 7.6%; cards under 50 votes → 0; median vote count of an on-screen card 168 → 781; empty More Like This rails 10 → 5; cast/crew cards carrying attribution 0% → 100%; TV pages whose people rail includes at least one series 0/40 → 23/40; cross-rail duplicates 0 → 0.** TWO THINGS THE RECOMMENDATIONS GOT WRONG, both caught by measuring rather than reasoning: (1) *backfill was underspecified* — "backfill in relevance order" let the one-vote LANDEaD back onto the rail at position 4, which defeats the entire fix; corrected to a two-tier floor (`MIN_RAIL_VOTES` 200 preferred, `ABSOLUTE_MIN_RAIL_VOTES` 50 hard) plus `MIN_RAIL_ITEMS` 2 so a rail hides rather than showing a single stub card. (2) **per-person credits created a NEW attractor worse than the one they replaced** — the first cut put *The Simpsons* on **16 of 100 pages** (vs Infinity War's 11), because long-running shows have enormous guest rosters and nearly every working actor has one Simpsons voice or one Grey's Anatomy episode; also "2 Broke Girls via Thomas Kail" for a single episode directed. Fixed with `TV_MIN_EPISODES = 5` on `combined_credits` TV entries (movies unaffected); top repeat fell to The Dark Knight ×5, a flat distribution. Cost of the floors: people rail now empty on 19/100 vs 9/100 — accepted on the trailer-feature precedent that an absent rail beats a misleading one. Device-verified on 4 titles: **Blade Runner 2049** (MLT → Blade Runner 1982 / Upgrade / Chappie; people → Prisoners *via Denis Villeneuve*, La La Land *via Ryan Gosling*, Empire Strikes Back *via Harrison Ford*); **Breaking Bad** — the TV case — (MLT → Better Call Saul / Narcos: Mexico / Snowfall, all series; people → Drive *via Bryan Cranston*, **Westworld** *via Aaron Paul*, Sully *via Anna Gunn*, proving `combined_credits` now returns TV on a TV page); **Oppenheimer** (Interstellar *via Nolan*, Dark Knight *via Cillian Murphy*, Edge of Tomorrow *via Emily Blunt*); **Dune: Part Two** (Prisoners *via Villeneuve*, Interstellar *via Chalamet*, No Way Home *via Zendaya*). TWO BUGS FOUND ONLY ON DEVICE, fixed and re-verified in a second build: (a) two-line titles pushed the year and attribution ~10px out of step with one-line neighbours — `minHeight: 38` was a raw constant while `bodyMd.lineHeight` is `scaleFont(20)`, i.e. device-scaled; now computed as `typography.bodyMd.lineHeight * 2` at render; (b) the score badge's `fontSize`/`fontWeight` were silently dead — `[styles.scoreSource, typography.labelSm]` puts the token last and RN style arrays let the last entry win, so the token overrode the local styles; order swapped. **NOT visually confirmed on device: the loading skeletons and the error/retry branch.** The skeletons became un-raceable precisely because rec 3 cut the fetch from 26 requests to 5 — it resolves faster than `adb input swipe` can reach the foot of the scroll; and an offline attempt to force the error branch failed because OkHttp served the rails from disk cache even after a force-stop (itself a useful finding). Both paths mirror the already-shipped Wikidata loading/error/retry pattern in the same component. Also confirmed the shipped bundle, not just the source: `with_people` and `moreFromCastAndCrew` are absent from `assets/index.android.bundle` and `combined_credits`/`viaPersonName` are present — worth doing, since a 30s "BUILD SUCCESSFUL" with 757 tasks up-to-date looks exactly like a build that skipped the JS bundle. `enrichDiscoverResults` was left alone — Discover still uses it. Not committed — changes sit uncommitted on branch `feat/soundtrack-cta` |
| 2026-07-24 | Results / ResultView | Hero ratings strip — 4-source critic badges (TMDb/IMDb/RT/Metacritic), each tappable to its source | Google movie knowledge-panel ratings cluster (RT icon-flip + Metacritic score box as secondary mirrors) | Four brand-perfect badges, two that lie about the score and two that link to a search box — a C-tier aggregator wearing A-tier logos | Approved | All 5 implemented and verified on release APK on the A54 (R5CW31BDEMH), branch `fix/ratings-strip-fidelity`. New `src/lib/ratingBadges.js` holds the slug/threshold logic (unit-tested, `tests/rating-badges.test.js`, 6 tests; suite 160/160, lint 0 errors). (1) RT + Metacritic now deep-link via title slug — RT underscores `/m/{slug}`, Metacritic hyphens `/movie/{slug}`, TV variants, search only when the title yields no slug; tapping RT on The Emoji Movie opened `rottentomatoes.com/m/the_emoji_movie` directly (was a search page). (2) tomato flips on the 60% line — verified red 🍅 at 93% (Killers of the Flower Moon) vs 🤢 splat at 6% (Emoji Movie). (3) Metacritic tile colored by threshold — verified green M at 89 (KOTFM) vs red M at 12 (Emoji Movie); yellow 40–60 takes dark text. (4) units restored via a `ScoreValue` sub-component with a dimmed suffix — `7.4/10`, `3.5/10`, `6%`, `12/100` all render. (5) a11y labels now carry the score + Fresh/Rotten ("…Rotten Tomatoes 6 percent, Rotten. Opens Rotten Tomatoes.") — code-verified (no uiautomator dump; mesh-orb loop blocks idle). Metacritic badge still sits under the floating share/bookmark buttons on long titles (pre-existing hero overlap, out of scope). Not committed — changes sit on the branch |
